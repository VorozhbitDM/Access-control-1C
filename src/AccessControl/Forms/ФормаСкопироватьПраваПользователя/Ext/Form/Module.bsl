
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	ЗапуститьСравнениеРолейПослеОткрытия = Ложь;
	
	ЭтотОбъект.Пользователь = ПараметрыСеанса.АвторизованныйПользователь;  
	Элементы.СравнитьРолиМетаданных.Доступность = Ложь;
	Элементы.ПоказатьВОбработке.Доступность = Ложь;
	
	ЗаполнитьТаблицуНастроек();
	
	СтрокаСвойств = ("Источник, Организация, Должность, ИмяКонфигурации");
	ЗаполнитьЗначенияСвойств(ЭтотОбъект, Параметры, СтрокаСвойств);
	
	ПодготовитьСравнениеРолейИзПараметров();
	
	Если НЕ ЗначениеЗаполнено(ИмяКонфигурации) Тогда
		ТекОбъект = РеквизитФормыВЗначение("Объект");
		ИмяКонфигурации = ТекОбъект.ОпределитьКонфигурацию();
	КонецЕсли;
	
	ЗаполнитьМетаЗначения();
			
КонецПроцедуры 

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	
	Если ЗапуститьСравнениеРолейПослеОткрытия Тогда
		Элементы.ГруппаСтраницы.ТекущаяСтраница = Элементы.ГруппаСтраницаСравнениеРолей;
		СравнитьРолиМетаданных(Неопределено);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ЭтотОбъект.Организация) Тогда
		ЭтотОбъект.УчитыватьОрганизацию = Истина;
		Элементы.ДекорацияОрганизация.Заголовок = Организация;
		Элементы.ДекорацияОрганизация.ЦветТекста = WebЦвета.Зеленый;
	Иначе
		Элементы.ДекорацияОрганизация.Заголовок = "Не удалось определить организацию";
		Элементы.ДекорацияОрганизация.ЦветТекста = WebЦвета.Красный;
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ЭтотОбъект.Должность) Тогда		
		ЭтотОбъект.УчитыватьДолжность = Истина;
		Элементы.ДекорацияДолжность.Заголовок = Должность;
		Элементы.ДекорацияДолжность.ЦветТекста = WebЦвета.Зеленый;
	Иначе
		Элементы.ДекорацияДолжность.Заголовок = "Не удалось определить должность";
		Элементы.ДекорацияДолжность.ЦветТекста = WebЦвета.Красный;
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Процедура ЗаполнитьМетаЗначения()
	
	ТекОбъект = РеквизитФормыВЗначение("Объект");
	ТекОбъект.ЗаполнитьСписокКоллекцийМетаданных(СписокМетаданных);
	ТекОбъект.ЗаполнитьТаблицуОбъектовМетаданных(ТаблицаКонфигурации);
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура СкопироватьПрава(Команда)
	
	Если НЕ ЗначениеЗаполнено(Источник) И НЕ ЗначениеЗаполнено(Приемник) Тогда
		ОбщегоНазначенияКлиентСервер.СообщитьПользователю("Укажите пользователей в качестве источника и приемника прав",, "Источник");
		Возврат;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(Источник) Тогда
		ОбщегоНазначенияКлиентСервер.СообщитьПользователю("Укажите пользователя в качестве источника прав",, "Источник");
		Возврат;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(Приемник) Тогда
		ОбщегоНазначенияКлиентСервер.СообщитьПользователю("Укажите пользователя в качестве приемника прав",, "Приемник");
		Возврат;
	КонецЕсли;
	
	ОО = Новый ОписаниеОповещения("ПослеПодтвержденияКопирования", ЭтотОбъект);
	ПоказатьВопрос(ОО, "Вы уверены, что хотите скопировать права?", РежимДиалогаВопрос.ДаНет,,, "Подтвердите действие");
	
КонецПроцедуры

&НаКлиенте
Процедура ПодобратьИсточник(Команда)
	
	Если НЕ ЗначениеЗаполнено(Источник) Тогда
		ОбщегоНазначенияКлиентСервер.СообщитьПользователю("Выберите пользователя в качестве источника прав");
		Возврат;
	КонецЕсли;
	
	Элементы.СравнениеРолей.Видимость = Ложь;
	Элементы.СравнениеГрупп.Видимость = Ложь;
	Элементы.СравнениеПрофилей.Видимость = Ложь;
	Элементы.ПользователиНаПодбор.Видимость = Истина;
	Элементы.ГруппаГоризонт.Видимость = Ложь;
	
	ПодобратьИсточникНаСервере();
	
КонецПроцедуры

&НаКлиенте
Процедура ВыбратьПриемникаПрав(Команда)
	
	ЭтотОбъект.Источник = Элементы.ПользователиНаПодбор.ТекущиеДанные.Пользователь;
	ПолучитьКадровыеДанныеИсточника();
	
КонецПроцедуры

&НаКлиенте
Процедура СравнитьПрава(Команда)
	
	Если НЕ ЗначениеЗаполнено(Источник) И НЕ ЗначениеЗаполнено(Приемник) Тогда
		ОбщегоНазначенияКлиентСервер.СообщитьПользователю("Выберите хотя бы одного пользователя, чтобы получить список прав");
		Возврат;				
	КонецЕсли;
	
	Элементы.ПользователиНаПодбор.Видимость = Ложь;
	Элементы.СравнениеРолей.Видимость = Истина;
	Элементы.СравнениеГрупп.Видимость = Истина;
	Элементы.СравнениеПрофилей.Видимость = Истина;
	Элементы.ГруппаГоризонт.Видимость = Истина;
	
	ИсточникЗаполнен = ЗначениеЗаполнено(Источник);
	ПриемникЗаполнен = ЗначениеЗаполнено(Приемник);
	
	Элементы.СравнениеРолейПользователь1.Видимость = ИсточникЗаполнен;
	Элементы.СравнениеГруппПользователь1.Видимость = ИсточникЗаполнен;
	Элементы.СравнениеПрофилейПользователь1.Видимость = ИсточникЗаполнен;
	
	Элементы.СравнениеРолейПользователь2.Видимость = ПриемникЗаполнен;
	Элементы.СравнениеГруппПользователь2.Видимость = ПриемникЗаполнен;
	Элементы.СравнениеПрофилейПользователь2.Видимость = ПриемникЗаполнен;
		
	СравнитьПраваНаСервере();
	
КонецПроцедуры

&НаКлиенте
Процедура ПоменятьМестами(Команда)
	
	Пользователь1 = Источник;
	Пользователь2 = Приемник;
	
	Источник = Пользователь2;
	Приемник = Пользователь1;
	
	ПолучитьКадровыеДанныеИсточника();
	
КонецПроцедуры

&НаКлиенте
Процедура ВыбратьРольДляОбработки(Команда)
	
	Если Элементы.СравнениеРолей.ТекущиеДанные = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	ДопПараметры = Новый Структура;
	ДопПараметры.Вставить("Значение", Элементы.СравнениеРолей.ТекущиеДанные.ИмяРоли);
	ДопПараметры.Вставить("Представление", Элементы.СравнениеРолей.ТекущиеДанные.Представление);
	
	ОО = Новый ОписаниеОповещения("ПослеВыбораРоли", ЭтотОбъект, ДопПараметры);
	ПоказатьВопрос(ОО, "Перенести роль в форму анализа ролей?", РежимДиалогаВопрос.ДаНет,, КодВозвратаДиалога.Да, "Выбор роли");
	
КонецПроцедуры

&НаКлиенте
Процедура ВыделитьСнятьВсе(Команда)
	
	ВариантОтметки = ?(Команда.Имя = "ОтметитьВсе", Истина, Ложь);
	
	Для Каждого Строка Из ВидыНастроек.ПолучитьЭлементы() Цикл
		
		Строка.Пометка = ВариантОтметки;
		
		Для Каждого СтрокаНастройки Из Строка.ПолучитьЭлементы() Цикл			
			СтрокаНастройки.Пометка = ВариантОтметки;				
		КонецЦикла;
				
	КонецЦикла;
		
КонецПроцедуры

&НаКлиенте
Процедура ОчиститьНастройки(Команда)
	
	Если НЕ ЗначениеЗаполнено(Пользователь) Тогда
		ОбщегоНазначенияКлиентСервер.СообщитьПользователю("Выберите пользователя",, "Пользователь");
		Возврат;
	КонецЕсли;
	
	НастройкиДляОчистки = Новый Массив;
	
	Для Каждого Строка Из ВидыНастроек.ПолучитьЭлементы() Цикл
		
		Для Каждого Настройка Из Строка.ПолучитьЭлементы() Цикл
						
			Если Настройка.Пометка Тогда
				НастройкиДляОчистки.Добавить(Новый Структура(
					"ВидНастроек, КлючОбъекта",
					СтрЗаменить(Строка.Имя, " ", ""),
					Настройка.Имя));
			КонецЕсли;
			
		КонецЦикла;
				
	КонецЦикла;
	
	Если НЕ НастройкиДляОчистки.Количество() Тогда
		ОбщегоНазначенияКлиентСервер.СообщитьПользователю("Выберите настройки для очистки");
		Возврат;			
	КонецЕсли;
	
	СброситьНастройкиНаСервере(НастройкиДляОчистки);
	
КонецПроцедуры

&НаКлиенте
Процедура СохранитьНастройки(Команда)
	
	ОО = Новый ОписаниеОповещения("ПослеВыбораКаталогаФайла", ЭтотОбъект);
	
	Диалог = Новый ДиалогВыбораФайла(РежимДиалогаВыбораФайла.Сохранение);
	Диалог.ПолноеИмяФайла = "Настройки [" + Пользователь + "]";
	Диалог.Заголовок = "Выберите каталог для сохранения файла";
	Диалог.Фильтр = "*.xml|*.xml";
	
	ФайловаяСистемаКлиент.ПоказатьДиалогВыбора(ОО, Диалог);
		
КонецПроцедуры 

&НаКлиенте
Процедура ВосстановитьНастройки(Команда)
	
	ОО = Новый ОписаниеОповещения("ПослеВыбораНастроек", ЭтотОбъект);
	
	Диалог = Новый ДиалогВыбораФайла(РежимДиалогаВыбораФайла.Открытие);
	Диалог.Заголовок = "Выберите файл с настройками";
	Диалог.Фильтр = "*.xml|*.xml";
	
	ФайловаяСистемаКлиент.ПоказатьДиалогВыбора(ОО, Диалог);
	
	КоллекцияСтрок = ВидыНастроек.ПолучитьЭлементы();
	КоллекцияСтрок.Очистить();
	
	ЗаполнитьТаблицуНастроек();
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовФормы

&НаКлиенте
Процедура УчитыватьДолжностьПриИзменении(Элемент)
	
	Если НЕ ЗначениеЗаполнено(ЭтотОбъект.Должность) Тогда		
		ОбщегоНазначенияКлиентСервер.СообщитьПользователю("Не удалось определить должность у источника прав");
		Возврат;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура УчитыватьОрганизациюПриИзменении(Элемент)
	
	Если НЕ ЗначениеЗаполнено(ЭтотОбъект.Организация) Тогда		
		ОбщегоНазначенияКлиентСервер.СообщитьПользователю("Не удалось определить организацию у источника прав");
		Возврат;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ИсточникПриИзменении(Элемент)
	
	ПолучитьКадровыеДанныеИсточника();
	
КонецПроцедуры

&НаКлиенте
Процедура ИмяФайлаОткрытие(Элемент, СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь;
	ЗапуститьПриложение(ИмяФайла);
	
КонецПроцедуры

&НаКлиенте
Процедура ПользовательПриИзменении(Элемент)
	
	КоллекцияСтрок = ВидыНастроек.ПолучитьЭлементы();
	КоллекцияСтрок.Очистить();
	
	ЗаполнитьТаблицуНастроек();
	
КонецПроцедуры

&НаКлиенте
Процедура ВидыНастроек1ПриИзменении(Элемент)
	
	Если Элемент.ТекущиеДанные.ПолучитьРодителя() = Неопределено Тогда
		
		Для Каждого Строка Из Элемент.ТекущиеДанные.ПолучитьЭлементы() Цикл			
			Строка.Пометка = Элемент.ТекущиеДанные.Пометка;				
		КонецЦикла;
		
	Иначе
		
		ТекРодитель = Элемент.ТекущиеДанные.ПолучитьРодителя();
		ТекРодитель.Пометка = Элемент.ТекущиеДанные.Пометка;
		
	КонецЕсли;	
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаСервере
Процедура ПодготовитьСравнениеРолейИзПараметров()
	
	Если НЕ Параметры.Свойство("РолиДляСравнения") Тогда
		Возврат;
	КонецЕсли;
	
	РолиДляСравнения = Параметры.РолиДляСравнения;
	
	Если ТипЗнч(РолиДляСравнения) <> Тип("Массив") Или РолиДляСравнения.Количество() < 2 Тогда
		Возврат;
	КонецЕсли;
	
	ЗаполнитьСписокРолей();
	
	МассивПредставлений = Новый Массив;
	
	Для Каждого ИмяРоли Из РолиДляСравнения Цикл
		
		ЭлементСписка = СписокРолейМетаданных.НайтиПоЗначению(ИмяРоли);
		
		Если ЭлементСписка <> Неопределено Тогда
			ЭлементСписка.Пометка = Истина;
		КонецЕсли;
		
		МетаРоль = Метаданные.Роли.Найти(ИмяРоли);
		
		Если МетаРоль <> Неопределено Тогда
			МассивПредставлений.Добавить(МетаРоль.Синоним);
		Иначе
			МассивПредставлений.Добавить(ИмяРоли);
		КонецЕсли;
		
	КонецЦикла;
	
	ВыбраннаяРоль = СтрСоединить(МассивПредставлений, ", ");
	Элементы.СравнитьРолиМетаданных.Доступность = Истина;
	
	Если Параметры.Свойство("ЗапуститьСравнениеРолейМетаданных") И Параметры.ЗапуститьСравнениеРолейМетаданных Тогда
		ЗапуститьСравнениеРолейПослеОткрытия = Истина;
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Процедура СравнитьПраваНаСервере()
	
	СравнитьРолиНаСервере();
	СравнитьГруппыДоступаНаСервере();
	СравнитьПрофилиНаСервере();
	
КонецПроцедуры

&НаСервере
Процедура ЗаполнитьТаблицуНастроек()
	
	ДобавитьСтроку(Ложь, "Настройки данных форм");
	ДобавитьСтроку(Ложь, "Общие настройки");
	ДобавитьСтроку(Ложь, "Настройки динамических списков");
	ДобавитьСтроку(Ложь, "Пользовательские настройки отчетов");
	ДобавитьСтроку(Ложь, "Системные настройки");
		
КонецПроцедуры

&НаСервере
Процедура ДобавитьСтроку(Пометка, Имя)
	
	НоваяСтрока = ВидыНастроек.ПолучитьЭлементы().Добавить();
	НоваяСтрока.Пометка = Пометка;
	НоваяСтрока.Имя     = Имя;
	                                  
	Выборка = ПолучитьХранилищеНастроек(Имя).Выбрать(Новый Структура("Пользователь", ПолучитьПользователяИБ(Пользователь).Имя));
	
	ВторойУровень = НоваяСтрока.ПолучитьЭлементы();
	
	Пока Выборка.Следующий() Цикл 
		СтрокаДерева = ВторойУровень.Добавить();
		СтрокаДерева.Имя = Выборка.КлючОбъекта;
	КонецЦикла;
	
КонецПроцедуры

&НаКлиенте
Процедура ПослеПодтвержденияКопирования(Ответ, ДополнительныеПараметры) Экспорт
	
	Если Ответ = КодВозвратаДиалога.Да Тогда
		
		Попытка
			СкопироватьПраваНаСервере();
			ПоказатьОповещениеПользователя("Успешно!",,, БиблиотекаКартинок.Успешно);
			Закрыть();
		Исключение
			ПоказатьОповещениеПользователя("Произошла ошибка",,, БиблиотекаКартинок.Ошибка32);
		КонецПопытки;
		
	КонецЕсли;
		
КонецПроцедуры

&НаСервере
Процедура СкопироватьПраваНаСервере()
	
	МодульУправлениеДоступомСлужебный = ОбщегоНазначения.ОбщийМодуль("УправлениеДоступомСлужебный");
	МодульУправлениеДоступомСлужебный.ПриКопированииПравНовомуПользователю(Источник, Приемник);
	ПользователиСлужебный.СкопироватьГруппыПользователя(Источник, Приемник);
		
КонецПроцедуры

&НаСервере
Процедура ПодобратьИсточникНаСервере()
	
	Запрос = Новый Запрос;
	Запрос.Текст = "ВЫБРАТЬ РАЗЛИЧНЫЕ
	               |	Сотрудники.Ссылка КАК Сотрудник,
				   |	Пользователи.Ссылка КАК Пользователь,
	               |	ФизическиеЛица.Ссылка КАК ФизЛицо,
	               |	Сотрудники.ГоловнаяОрганизация КАК Организация,
	               |	КадроваяИсторияСотрудниковСрезПоследних.Должность КАК Должность
	               |ИЗ
	               |	Справочник.Пользователи КАК Пользователи
	               |		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.ФизическиеЛица КАК ФизическиеЛица
	               |			ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.Сотрудники КАК Сотрудники
	               |			ПО (Сотрудники.ФизическоеЛицо = ФизическиеЛица.Ссылка)
	               |			ПРАВОЕ СОЕДИНЕНИЕ РегистрСведений.КадроваяИсторияСотрудников.СрезПоследних КАК КадроваяИсторияСотрудниковСрезПоследних
	               |			ПО (КадроваяИсторияСотрудниковСрезПоследних.ФизическоеЛицо = ФизическиеЛица.Ссылка)
	               |		ПО Пользователи.ФизическоеЛицо = ФизическиеЛица.Ссылка
	               |ГДЕ
	               |	Пользователи.Ссылка <> &Источник
				   |	И КадроваяИсторияСотрудниковСрезПоследних.Должность = &Должность
	               |	И Сотрудники.ГоловнаяОрганизация = &Организация";
	
	Запрос.УстановитьПараметр("Источник", Источник);
	
	Если УчитыватьОрганизацию И УчитыватьДолжность Тогда		
		Запрос.УстановитьПараметр("Организация", Организация);
		Запрос.УстановитьПараметр("Должность", Должность);		
	ИначеЕсли УчитыватьОрганизацию И НЕ УчитыватьДолжность Тогда		
		Запрос.Текст = СтрЗаменить(Запрос.Текст, "И КадроваяИсторияСотрудниковСрезПоследних.Должность = &Должность", "");
		Запрос.УстановитьПараметр("Организация", Организация);
	ИначеЕсли НЕ УчитыватьОрганизацию И УчитыватьДолжность Тогда		
		Запрос.Текст = СтрЗаменить(Запрос.Текст, "И Сотрудники.ГоловнаяОрганизация = &Организация", "");
		Запрос.УстановитьПараметр("Должность", Должность);
	Иначе
		ОбщегоНазначенияКлиентСервер.СообщитьПользователю("Подбор источника прав имеет смысл только с использованием отборов");
		Элементы.ПользователиНаПодбор.Видимость = Ложь;
		Возврат;
	КонецЕсли;
	
	Результат = Запрос.Выполнить().Выгрузить();
	
	Если НЕ Результат.Количество() Тогда		
		ОбщегоНазначенияКлиентСервер.СообщитьПользователю("К сожалению, похожий пользователь не найден, измените отборы и попробуйте снова");
		Элементы.ПользователиНаПодбор.Видимость = Ложь;
		Возврат;
	КонецЕсли;
	
	ПользователиНаПодбор.Загрузить(Запрос.Выполнить().Выгрузить());
	Элементы.ПользователиНаПодбор.Видимость = Истина;
	
КонецПроцедуры

&НаСервере
Процедура ПолучитьКадровыеДанныеИсточника()
	
	Если Метаданные.НайтиПоПолномуИмени("Справочник.Сотрудники") = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ ПЕРВЫЕ 1
	|	Сотрудники.Ссылка КАК Сотрудник,
	|	ФизическиеЛица.Ссылка КАК ФизЛицо,
	|	Пользователи.Ссылка КАК Пользователь,
	|	Сотрудники.ГоловнаяОрганизация КАК ГоловнаяОрганизация,
	|	КадроваяИсторияСотрудниковСрезПоследних.Должность КАК Должность
	|ИЗ
	|	Справочник.Пользователи КАК Пользователи
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.ФизическиеЛица КАК ФизическиеЛица
	|			ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.Сотрудники КАК Сотрудники
	|			ПО (Сотрудники.ФизическоеЛицо = ФизическиеЛица.Ссылка)
	|			ПРАВОЕ СОЕДИНЕНИЕ РегистрСведений.КадроваяИсторияСотрудников.СрезПоследних КАК КадроваяИсторияСотрудниковСрезПоследних
	|			ПО (КадроваяИсторияСотрудниковСрезПоследних.ФизическоеЛицо = ФизическиеЛица.Ссылка)
	|		ПО Пользователи.ФизическоеЛицо = ФизическиеЛица.Ссылка
	|ГДЕ
	|	Пользователи.Ссылка = &Источник";
	
	Запрос.УстановитьПараметр("Источник", ЭтотОбъект.Источник);
	
	Результат = Запрос.Выполнить();
	
	Если Результат.Пустой() Тогда
		Возврат;
	КонецЕсли;
	
	ТаблицаЗапроса = Результат.Выгрузить();
	
	ЭтотОбъект.Организация = ТаблицаЗапроса[0].ГоловнаяОрганизация;
	ЭтотОбъект.Должность   = ТаблицаЗапроса[0].Должность;
	
	Если УчитыватьОрганизацию <> Справочники.Организации.ПустаяСсылка() Тогда
		ЭтотОбъект.УчитыватьОрганизацию          = Истина;
		Элементы.ДекорацияОрганизация.Заголовок  = Строка(ЭтотОбъект.Организация);
		Элементы.ДекорацияОрганизация.ЦветТекста = WebЦвета.Зеленый;
	Иначе
		ЭтотОбъект.УчитыватьОрганизацию          = Ложь;
		Элементы.ДекорацияОрганизация.Заголовок  = "Не удалось определить организацию";
		Элементы.ДекорацияОрганизация.ЦветТекста = WebЦвета.Красный;
	КонецЕсли;
	
	Если УчитыватьДолжность <> Справочники.Должности.ПустаяСсылка() Тогда
		ЭтотОбъект.УчитыватьДолжность          = Истина;
		Элементы.ДекорацияДолжность.Заголовок  = Строка(ЭтотОбъект.Должность);
		Элементы.ДекорацияДолжность.ЦветТекста = WebЦвета.Зеленый;		
	Иначе
		ЭтотОбъект.УчитыватьДолжность          = Ложь;
		Элементы.ДекорацияДолжность.Заголовок  = "Не удалось определить должность";
		Элементы.ДекорацияДолжность.ЦветТекста = WebЦвета.Красный;
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Функция ТекстУсловияПользовательВГруппеДоступа(ИмяПараметраПользователя)
	
	Возврат "ИСТИНА В
	|			(ВЫБРАТЬ ПЕРВЫЕ 1
	|				ИСТИНА
	|			ИЗ
	|				Справочник.ГруппыДоступа.Пользователи КАК ПользователиГруппДоступа
	|			ГДЕ
	|				ПользователиГруппДоступа.Ссылка = ГруппыДоступа.Ссылка
	|				И НЕ(ПользователиГруппДоступа.Пользователь <> &" + ИмяПараметраПользователя + "
	|						И НЕ ПользователиГруппДоступа.Пользователь В
	|								(ВЫБРАТЬ
	|									СоставыГруппПользователей.ГруппаПользователей
	|								ИЗ
	|									РегистрСведений.СоставыГруппПользователей КАК СоставыГруппПользователей
	|								ГДЕ
	|									СоставыГруппПользователей.Пользователь = &" + ИмяПараметраПользователя + ")))";
	
КонецФункции

&НаСервере
Процедура СравнитьПрофилиНаСервере()
	
	УсловиеЧленства1 = ТекстУсловияПользовательВГруппеДоступа("Пользователь1");
	УсловиеЧленства2 = ТекстУсловияПользовательВГруппеДоступа("Пользователь2");
	
	Запрос = Новый Запрос;
	Запрос.Текст = "ВЫБРАТЬ РАЗЛИЧНЫЕ
	               |	&Пользователь1 КАК Пользователь,
	               |	ГруппыДоступа.Профиль КАК Профиль
	               |ПОМЕСТИТЬ ВТ_Пользователь1
	               |ИЗ
	               |	Справочник.ГруппыДоступа КАК ГруппыДоступа
	               |ГДЕ
	               |	НЕ ГруппыДоступа.ПометкаУдаления
	               |	И НЕ ГруппыДоступа.Профиль.ПометкаУдаления
	               |	И " + УсловиеЧленства1 + "
	               |;
	               |
	               |////////////////////////////////////////////////////////////////////////////////
	               |ВЫБРАТЬ РАЗЛИЧНЫЕ
	               |	&Пользователь2 КАК Пользователь,
	               |	ГруппыДоступа.Профиль КАК Профиль
	               |ПОМЕСТИТЬ ВТ_Пользователь2
	               |ИЗ
	               |	Справочник.ГруппыДоступа КАК ГруппыДоступа
	               |ГДЕ
	               |	НЕ ГруппыДоступа.ПометкаУдаления
	               |	И НЕ ГруппыДоступа.Профиль.ПометкаУдаления
	               |	И " + УсловиеЧленства2 + "
	               |;
	               |
	               |////////////////////////////////////////////////////////////////////////////////
	               |ВЫБРАТЬ
	               |	ЕСТЬNULL(ВТ_Пользователь1.Профиль, ВТ_Пользователь2.Профиль) КАК Профиль,
	               |	ИСТИНА КАК КартинкаПрофиля,
	               |	ЕСТЬNULL(ВТ_Пользователь1.Профиль.Наименование, ВТ_Пользователь2.Профиль.Наименование) КАК ПредставлениеПрофиля,
	               |	ВЫБОР
	               |		КОГДА ВТ_Пользователь1.Пользователь ЕСТЬ NULL
	               |			ТОГДА ЛОЖЬ
	               |		ИНАЧЕ ИСТИНА
	               |	КОНЕЦ КАК Пользователь1,
	               |	ВЫБОР
	               |		КОГДА ВТ_Пользователь2.Пользователь ЕСТЬ NULL
	               |			ТОГДА ЛОЖЬ
	               |		ИНАЧЕ ИСТИНА
	               |	КОНЕЦ КАК Пользователь2,
	               |	ВЫБОР
	               |		КОГДА НЕ ВТ_Пользователь1.Пользователь ЕСТЬ NULL
	               |				И НЕ ВТ_Пользователь2.Пользователь ЕСТЬ NULL
	               |			ТОГДА ИСТИНА
	               |		ИНАЧЕ ЛОЖЬ
	               |	КОНЕЦ КАК СовпадениеПрофилей,
				   |	ЕСТЬNULL(ВТ_Пользователь1.Профиль.Наименование, ВТ_Пользователь2.Профиль.Наименование) КАК ИмяПрофиля
	               |ИЗ
	               |	ВТ_Пользователь2 КАК ВТ_Пользователь2
	               |		ПОЛНОЕ СОЕДИНЕНИЕ ВТ_Пользователь1 КАК ВТ_Пользователь1
	               |		ПО (ВТ_Пользователь1.Профиль = ВТ_Пользователь2.Профиль)
	               |
	               |УПОРЯДОЧИТЬ ПО
	               |	СовпадениеПрофилей УБЫВ,
	               |	Пользователь1 УБЫВ,
	               |	Пользователь2 УБЫВ";
	
	Запрос.УстановитьПараметр("Пользователь1", Источник);
	Запрос.УстановитьПараметр("Пользователь2", Приемник);
	
	СравнениеПрофилей.Загрузить(Запрос.Выполнить().Выгрузить());
	
	Элементы.СравнениеПрофилей.ПодчиненныеЭлементы.СравнениеПрофилейПользователь1.Заголовок = ФизическиеЛицаКлиентСервер.ФамилияИнициалы(Источник.Наименование);
	Элементы.СравнениеПрофилей.ПодчиненныеЭлементы.СравнениеПрофилейПользователь2.Заголовок = ФизическиеЛицаКлиентСервер.ФамилияИнициалы(Приемник.Наименование); 
	
	Элементы.СтраницаСравнениеПрофилей.Заголовок = "По профилям (" + СравнениеПрофилей.Количество() + ")";
	
КонецПроцедуры

&НаСервере
Процедура СравнитьГруппыДоступаНаСервере()
	
	УсловиеЧленства1 = ТекстУсловияПользовательВГруппеДоступа("Пользователь1");
	УсловиеЧленства2 = ТекстУсловияПользовательВГруппеДоступа("Пользователь2");
	
	Запрос = Новый Запрос;
	Запрос.Текст = "ВЫБРАТЬ РАЗЛИЧНЫЕ
	               |	&Пользователь1 КАК Пользователь,
	               |	ГруппыДоступа.Ссылка КАК ГруппаДоступа
	               |ПОМЕСТИТЬ ВТ_Пользователь1
	               |ИЗ
	               |	Справочник.ГруппыДоступа КАК ГруппыДоступа
	               |ГДЕ
	               |	НЕ ГруппыДоступа.ПометкаУдаления
	               |	И " + УсловиеЧленства1 + "
	               |;
	               |
	               |////////////////////////////////////////////////////////////////////////////////
	               |ВЫБРАТЬ РАЗЛИЧНЫЕ
	               |	&Пользователь2 КАК Пользователь,
	               |	ГруппыДоступа.Ссылка КАК ГруппаДоступа
	               |ПОМЕСТИТЬ ВТ_Пользователь2
	               |ИЗ
	               |	Справочник.ГруппыДоступа КАК ГруппыДоступа
	               |ГДЕ
	               |	НЕ ГруппыДоступа.ПометкаУдаления
	               |	И " + УсловиеЧленства2 + "
	               |;
	               |
	               |////////////////////////////////////////////////////////////////////////////////
	               |ВЫБРАТЬ
	               |	ЕСТЬNULL(ВТ_Пользователь1.ГруппаДоступа, ВТ_Пользователь2.ГруппаДоступа) КАК ГруппаДоступа,
	               |	ИСТИНА КАК КартинкаГруппыДоступа,
	               |	ЕСТЬNULL(ВТ_Пользователь1.ГруппаДоступа.Наименование, ВТ_Пользователь2.ГруппаДоступа.Наименование) КАК ПредставлениеГруппыДоступа,
	               |	ВЫБОР
	               |		КОГДА ВТ_Пользователь1.Пользователь ЕСТЬ NULL
	               |			ТОГДА ЛОЖЬ
	               |		ИНАЧЕ ИСТИНА
	               |	КОНЕЦ КАК Пользователь1,
	               |	ВЫБОР
	               |		КОГДА ВТ_Пользователь2.Пользователь ЕСТЬ NULL
	               |			ТОГДА ЛОЖЬ
	               |		ИНАЧЕ ИСТИНА
	               |	КОНЕЦ КАК Пользователь2,
	               |	ВЫБОР
	               |		КОГДА НЕ ВТ_Пользователь1.Пользователь ЕСТЬ NULL
	               |				И НЕ ВТ_Пользователь2.Пользователь ЕСТЬ NULL
	               |			ТОГДА ИСТИНА
	               |		ИНАЧЕ ЛОЖЬ
	               |	КОНЕЦ КАК СовпадениеГруппДоступа,
				   |	ЕСТЬNULL(ВТ_Пользователь1.ГруппаДоступа.Наименование, ВТ_Пользователь2.ГруппаДоступа.Наименование) КАК ИмяГруппыДоступа
	               |ИЗ
	               |	ВТ_Пользователь2 КАК ВТ_Пользователь2
	               |		ПОЛНОЕ СОЕДИНЕНИЕ ВТ_Пользователь1 КАК ВТ_Пользователь1
	               |		ПО (ВТ_Пользователь1.ГруппаДоступа = ВТ_Пользователь2.ГруппаДоступа)
	               |
	               |УПОРЯДОЧИТЬ ПО
	               |	СовпадениеГруппДоступа УБЫВ,
	               |	Пользователь1 УБЫВ,
	               |	Пользователь2 УБЫВ";
	
	Запрос.УстановитьПараметр("Пользователь1", Источник);
	Запрос.УстановитьПараметр("Пользователь2", Приемник);
	
	СравнениеГрупп.Загрузить(Запрос.Выполнить().Выгрузить());
	
	Элементы.СравнениеГрупп.ПодчиненныеЭлементы.СравнениеГруппПользователь1.Заголовок = ФизическиеЛицаКлиентСервер.ФамилияИнициалы(Источник.Наименование);
	Элементы.СравнениеГрупп.ПодчиненныеЭлементы.СравнениеГруппПользователь2.Заголовок = ФизическиеЛицаКлиентСервер.ФамилияИнициалы(Приемник.Наименование);
	
	Элементы.СтраницаСравнениеГрупп.Заголовок = "По группам доступа (" + СравнениеГрупп.Количество() + ")";
	
КонецПроцедуры

&НаСервере
Процедура СравнитьРолиНаСервере()
	
	УсловиеЧленства1 = ТекстУсловияПользовательВГруппеДоступа("Пользователь1");
	УсловиеЧленства2 = ТекстУсловияПользовательВГруппеДоступа("Пользователь2");
		
	Запрос = Новый Запрос;
	Запрос.Текст = "ВЫБРАТЬ РАЗЛИЧНЫЕ
	               |	&Пользователь1 КАК Пользователь,
	               |	ПрофилиГруппДоступаРоли.Роль КАК Роль
	               |ПОМЕСТИТЬ ВТ_Пользователь1
	               |ИЗ
	               |	Справочник.ГруппыДоступа КАК ГруппыДоступа
	               |		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.ПрофилиГруппДоступа.Роли КАК ПрофилиГруппДоступаРоли
	               |		ПО ГруппыДоступа.Профиль = ПрофилиГруппДоступаРоли.Ссылка
	               |ГДЕ
	               |	НЕ ГруппыДоступа.ПометкаУдаления
	               |	И НЕ ПрофилиГруппДоступаРоли.Ссылка.ПометкаУдаления
	               |	И " + УсловиеЧленства1 + "
	               |;
	               |
	               |////////////////////////////////////////////////////////////////////////////////
	               |ВЫБРАТЬ РАЗЛИЧНЫЕ
	               |	&Пользователь2 КАК Пользователь,
	               |	ПрофилиГруппДоступаРоли.Роль КАК Роль
	               |ПОМЕСТИТЬ ВТ_Пользователь2
	               |ИЗ
	               |	Справочник.ГруппыДоступа КАК ГруппыДоступа
	               |		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.ПрофилиГруппДоступа.Роли КАК ПрофилиГруппДоступаРоли
	               |		ПО ГруппыДоступа.Профиль = ПрофилиГруппДоступаРоли.Ссылка
	               |ГДЕ
	               |	НЕ ГруппыДоступа.ПометкаУдаления
	               |	И НЕ ПрофилиГруппДоступаРоли.Ссылка.ПометкаУдаления
	               |	И " + УсловиеЧленства2 + "
	               |;
	               |
	               |////////////////////////////////////////////////////////////////////////////////
	               |ВЫБРАТЬ
	               |	ЕСТЬNULL(ВТ_Пользователь1.Роль, ВТ_Пользователь2.Роль) КАК Роль,
	               |	ИСТИНА КАК КартинкаРоли,
	               |	ЕСТЬNULL(ВТ_Пользователь1.Роль.Синоним, ВТ_Пользователь2.Роль.Синоним) КАК Представление,
	               |	ВЫБОР
	               |		КОГДА ВТ_Пользователь1.Пользователь ЕСТЬ NULL
	               |			ТОГДА ЛОЖЬ
	               |		ИНАЧЕ ИСТИНА
	               |	КОНЕЦ КАК Пользователь1,
	               |	ВЫБОР
	               |		КОГДА ВТ_Пользователь2.Пользователь ЕСТЬ NULL
	               |			ТОГДА ЛОЖЬ
	               |		ИНАЧЕ ИСТИНА
	               |	КОНЕЦ КАК Пользователь2,
	               |	ВЫБОР
	               |		КОГДА НЕ ВТ_Пользователь1.Пользователь ЕСТЬ NULL
	               |				И НЕ ВТ_Пользователь2.Пользователь ЕСТЬ NULL
	               |			ТОГДА ИСТИНА
	               |		ИНАЧЕ ЛОЖЬ
	               |	КОНЕЦ КАК СовпадениеРолей,
	               |	ЕСТЬNULL(ВТ_Пользователь1.Роль.Имя, ВТ_Пользователь2.Роль.Имя) КАК ИмяРоли
	               |ИЗ
	               |	ВТ_Пользователь2 КАК ВТ_Пользователь2
	               |		ПОЛНОЕ СОЕДИНЕНИЕ ВТ_Пользователь1 КАК ВТ_Пользователь1
	               |		ПО (ВТ_Пользователь1.Роль = ВТ_Пользователь2.Роль)
	               |
	               |УПОРЯДОЧИТЬ ПО
	               |	СовпадениеРолей УБЫВ,
	               |	Пользователь1 УБЫВ,
	               |	Пользователь2 УБЫВ";
	
	Запрос.УстановитьПараметр("Пользователь1", Источник);
	Запрос.УстановитьПараметр("Пользователь2", Приемник);
	
	СравнениеРолей.Загрузить(Запрос.Выполнить().Выгрузить());
	
	Элементы.СравнениеРолей.ПодчиненныеЭлементы.СравнениеРолейПользователь1.Заголовок = ФизическиеЛицаКлиентСервер.ФамилияИнициалы(Источник.Наименование);
	Элементы.СравнениеРолей.ПодчиненныеЭлементы.СравнениеРолейПользователь2.Заголовок = ФизическиеЛицаКлиентСервер.ФамилияИнициалы(Приемник.Наименование);
	
	Элементы.СтраницаСравненияРолей.Заголовок = "По ролям (" + Формат(СравнениеРолей.Количество(), "ЧГ=0") + ")"; 
	
КонецПроцедуры

&НаКлиенте
Процедура ПослеВыбораРоли(Ответ, ДополнительныеПараметры) Экспорт
	
	Если Ответ = КодВозвратаДиалога.Да Тогда
		Закрыть(ДополнительныеПараметры);
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Процедура СброситьНастройкиНаСервере(НастройкиДляОчистки)
	
	ПользовательИБ = ПолучитьПользователяИБ(Пользователь);
	
	Если ПользовательИБ = Неопределено Тогда
		ОбщегоНазначенияКлиентСервер.СообщитьПользователю(
			"Не удалось найти пользователя информационной базы для выбранного пользователя", , "Пользователь");
		Возврат;
	КонецЕсли;
	
	Для Каждого Настройка Из НастройкиДляОчистки Цикл
		Хранилище = ПолучитьХранилищеНастроек(Настройка.ВидНастроек);
		ОчиститьХранилищеНастроек(Хранилище, ПользовательИБ, Настройка.КлючОбъекта);
	КонецЦикла;
	
	КоллекцияСтрок = ВидыНастроек.ПолучитьЭлементы();
	КоллекцияСтрок.Очистить();
	
	ЗаполнитьТаблицуНастроек();
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ПолучитьПользователяИБ(Пользователь)
	
	Если Пользователь.Метаданные().Реквизиты.Найти("ИдентификаторПользователяИБ") <> Неопределено Тогда
		ПользовательИБ = ПользователиИнформационнойБазы.НайтиПоУникальномуИдентификатору(Пользователь.ИдентификаторПользователяИБ);
	Иначе
		ПользовательИБ = ПользователиИнформационнойБазы.НайтиПоИмени(СокрЛП(Пользователь.Код));
	КонецЕсли;
	
	Возврат ПользовательИБ;
	
КонецФункции

&НаСервере
Функция ПолучитьХранилищеНастроек(Имя)
	
	Хранилище = Неопределено;
	
	Если Имя = "Варианты отчетов"
		ИЛИ Имя = "Вариантыотчетов" Тогда
		
		Хранилище = Вычислить("ХранилищеВариантовОтчетов");
		
	ИначеЕсли Имя = "Настройки данных форм"
		ИЛИ Имя = "Настройкиданныхформ" Тогда
		
		Хранилище = Вычислить("ХранилищеНастроекДанныхФорм");
		
	ИначеЕсли Имя = "Общие настройки"
		ИЛИ Имя = "Общиенастройки" Тогда
		
		Хранилище = Вычислить("ХранилищеОбщихНастроек");
		
	ИначеЕсли Имя = "Настройки динамических списков"
		ИЛИ Имя = "Настройкидинамическихсписков" Тогда
		
		Хранилище = Вычислить("ХранилищеПользовательскихНастроекДинамическихСписков");
		
	ИначеЕсли Имя = "Пользовательские настройки отчетов"
		ИЛИ Имя = "Пользовательскиенастройкиотчетов" Тогда
		
		Хранилище = Вычислить("ХранилищеПользовательскихНастроекОтчетов");
		
	ИначеЕсли Имя = "Системные настройки"
		ИЛИ Имя = "Системныенастройки" Тогда
		
		Хранилище = Вычислить("ХранилищеСистемныхНастроек");
		
	КонецЕсли;	
	
	Возврат Хранилище;
	
КонецФункции

&НаСервере
Процедура ОчиститьХранилищеНастроек(Хранилище, ПользовательИБ, КлючОбъекта)
	
	Если Хранилище = Неопределено Тогда
		ОбщегоНазначенияКлиентСервер.СообщитьПользователю("Не удалось определить хранилище настроек для очистки");
		Возврат;
	КонецЕсли;
	
	Отбор = Новый Структура;
	Отбор.Вставить("Пользователь", ПользовательИБ.Имя);
	Отбор.Вставить("КлючОбъекта", КлючОбъекта);
	
	Попытка
		Выборка = Хранилище.Выбрать(Отбор);
	Исключение
		ОбщегоНазначенияКлиентСервер.СообщитьПользователю(ОписаниеОшибки());
		Возврат;
	КонецПопытки;
	
	Пока Выборка.Следующий() Цикл
		Хранилище.Удалить(Выборка.КлючОбъекта, Выборка.КлючНастроек, Выборка.Пользователь);
	КонецЦикла;
	
КонецПроцедуры

&НаКлиенте
Процедура ПослеВыбораКаталогаФайла(Результат, ДополнительныеПараметры) Экспорт
	
	Если Результат = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	ИмяФайла = Результат[0];
	
	АдресФайла = СохранитьНастройкиНаСервере(Истина, УникальныйИдентификатор);
	ПолучитьФайл(АдресФайла, ИмяФайла, Ложь);
	
	Элементы.ИмяФайла.Заголовок = "Сохранено в";
	Элементы.ИмяФайла.Видимость = Истина;
	
КонецПроцедуры

&НаСервере	
Функция СохранитьНастройкиНаСервере(ПередаватьНаКлиента = Ложь, УникальныйИдентификатор = Неопределено)
	
	Если ПередаватьНаКлиента Тогда
		пИмяФайла = ПолучитьИмяВременногоФайла("xml");
	Иначе	
		пИмяФайла = ИмяФайла;
	КонецЕсли;	
	
	ПользовательИБ = ПолучитьПользователяИБ(Пользователь);
	
	ЗаписьXML = Новый ЗаписьXML;
	ЗаписьXML.ОткрытьФайл(пИмяФайла);
	ЗаписьXML.ЗаписатьНачалоЭлемента("Корень");
	
	Для Каждого СтрокаТЧ Из ВидыНастроек.ПолучитьЭлементы() Цикл
		
		Если НЕ СтрокаТЧ.Пометка Тогда
			Продолжить;
		КонецЕсли;	
		
		Попытка
			Хранилище = ПолучитьХранилищеНастроек(СтрокаТЧ.Имя);
		Исключение
			Сообщить(ОписаниеОшибки());
			Продолжить;
		КонецПопытки;	
		
		Если Хранилище <> Неопределено Тогда
			СохранитьХранилищеНастроек(ЗаписьXML, Хранилище, СтрЗаменить(СтрокаТЧ.Имя, " ", ""), ПользовательИБ);
		КонецЕсли;	
			
	КонецЦикла;	
	
	ЗаписьXML.ЗаписатьКонецЭлемента();
	ЗаписьXML.Закрыть();
	
	Если ПередаватьНаКлиента Тогда
		
		ДвоичныеДанные = Новый ДвоичныеДанные(пИмяФайла);
		Адрес = ПоместитьВоВременноеХранилище(ДвоичныеДанные, УникальныйИдентификатор);
		Возврат Адрес;
		
	КонецЕсли;	
	
КонецФункции

&НаСервере
Процедура СохранитьХранилищеНастроек(ЗаписьXML, Хранилище, Имя, ПользовательИБ)
	
	ЗаписьXML.ЗаписатьНачалоЭлемента(Имя);
	
	Отбор = Новый Структура;
	Отбор.Вставить("Пользователь", ПользовательИБ.Имя);
	
	Выборка = Хранилище.Выбрать(Отбор);
	Пока Выборка.Следующий() Цикл
		
		ТипЗначения = ТипЗнч(Выборка.Настройки);
		
		ЗаписьXML.ЗаписатьНачалоЭлемента("Элемент");
		
		ЗаписатьЭлементXML(ЗаписьXML, "КлючНастроек",  Выборка.КлючНастроек);
		ЗаписатьЭлементXML(ЗаписьXML, "КлючОбъекта",   Выборка.КлючОбъекта);
		ЗаписатьЭлементXML(ЗаписьXML, "Пользователь",  Выборка.Пользователь);
		ЗаписатьЭлементXML(ЗаписьXML, "Представление", Выборка.Представление);
		
		ЗаписьXML.ЗаписатьНачалоЭлемента("Значение");
		
		Если ТипЗначения = Тип("Строка") ИЛИ ТипЗначения = Тип("Число") ИЛИ ТипЗначения = Тип("Булево") Тогда			
			ЗаписатьXML(ЗаписьXML, Выборка.Настройки);			
		Иначе				
			Хранилище = Новый ХранилищеЗначения(Выборка.Настройки);
			ЗаписатьXML(ЗаписьXML, Хранилище);			
		КонецЕсли;	
			
		ЗаписьXML.ЗаписатьКонецЭлемента();
			
		ЗаписьXML.ЗаписатьКонецЭлемента();
		
	КонецЦикла;
	
	ЗаписьXML.ЗаписатьКонецЭлемента();
	
КонецПроцедуры

&НаСервере
Процедура ЗаписатьЭлементXML(ЗаписьXML, Имя, Значение)
	
	ЗаписьXML.ЗаписатьНачалоЭлемента(Имя);
	ЗаписьXML.ЗаписатьТекст(XMLСтрока(Значение));
	ЗаписьXML.ЗаписатьКонецЭлемента();
	
КонецПроцедуры

&НаКлиенте
Процедура ПослеВыбораНастроек(Результат, ДополнительныеПараметры) Экспорт
	
	Если Результат = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	ИмяФайла = Результат[0];
	
	АдресФайла = "";
    ПоместитьФайл(АдресФайла, ИмяФайла,, Ложь, УникальныйИдентификатор);
	
	ВосстановитьНастройкиНаСервере(АдресФайла);
	
	Элементы.ИмяФайла.Заголовок = "Восстановлено из:";
	Элементы.ИмяФайла.Видимость = Истина;

КонецПроцедуры

&НаСервере
Процедура ВосстановитьНастройкиНаСервере(АдресФайла = Неопределено)
	
	ЧтениеXML = Новый ЧтениеXML;
	
	Если АдресФайла = Неопределено Тогда
		ЧтениеXML.ОткрытьФайл(ИмяФайла);
	Иначе	
		ИмяВременногоФайла = ПолучитьИмяВременногоФайла("xml");
		
		ДвоичныеДанные = ПолучитьИзВременногоХранилища(АдресФайла);
		ДвоичныеДанные.Записать(ИмяВременногоФайла);
		
		ЧтениеXML.ОткрытьФайл(ИмяВременногоФайла);
	КонецЕсли;	
	
	Хранилище = Неопределено;
		
	Пока ЧтениеXML.Прочитать() Цикл
		
		Если ЧтениеXML.ТипУзла = ТипУзлаXML.НачалоЭлемента Тогда
			
			ИмяЭлемента = ЧтениеXML.Имя;
			
			Попытка
				ТекХранилище = ПолучитьХранилищеНастроек(ИмяЭлемента);
			Исключение
				Продолжить;
			КонецПопытки;	
		
			Если ТекХранилище <> Неопределено Тогда
				Хранилище = ТекХранилище;
			КонецЕсли;
			
			Если ИмяЭлемента = "Элемент" Тогда
				
				СтруктураДанных = Новый Структура("КлючНастроек, КлючОбъекта, Пользователь, Представление, Значение", "", "", "", "", Неопределено);
				
			ИначеЕсли ИмяЭлемента = "Значение" Тогда
				
				ЧтениеXML.Прочитать();
				Значение = ПрочитатьXML(ЧтениеXML);
				
				Если ТипЗнч(Значение) = Тип("ХранилищеЗначения") Тогда
					СтруктураДанных.Вставить(ИмяЭлемента, Значение.Получить());
				Иначе	
					СтруктураДанных.Вставить(ИмяЭлемента, Значение);
				КонецЕсли;	
				
			КонецЕсли;	
			
		ИначеЕсли ЧтениеXML.ТипУзла = ТипУзлаXML.Текст Тогда
			
			СтруктураДанных.Вставить(ИмяЭлемента, ЧтениеXML.Значение);
			
		ИначеЕсли ЧтениеXML.ТипУзла = ТипУзлаXML.КонецЭлемента Тогда
			
			Если ЧтениеXML.Имя = "Элемент" Тогда
				
				Если Хранилище = Неопределено Тогда
					Продолжить;
				КонецЕсли;
		
				Хранилище.Сохранить(
					СтруктураДанных.КлючОбъекта,
					СтруктураДанных.КлючНастроек,
					СтруктураДанных.Значение,
					СтруктураДанных.Представление,
					СтруктураДанных.Пользователь);
				
			КонецЕсли;	
			
		КонецЕсли;	
		
	КонецЦикла;	
	
КонецПроцедуры

&НаСервере
Процедура ЗаполнитьСписокРолей()
	
	МассивРолей = Новый Массив;
	
	Для Каждого Роль Из Метаданные.Роли Цикл
		СписокРолейМетаданных.Добавить(Роль.Имя, Роль.Синоним,, БиблиотекаКартинок.ЗакрытыйКлюч);	
	КонецЦикла;		
	
КонецПроцедуры

&НаСервере
Функция ПолучитьСписокРолейДляОтметкиНаСервере(СтрокаПоиска)
	
	СписокДляОтметки = Новый СписокЗначений;
	СтрокаПоискаВРег = ВРег(СокрЛП(СтрокаПоиска));
	
	Для Каждого ЭлементСписка Из СписокРолейМетаданных Цикл
		
		Если НЕ ПустаяСтрока(СтрокаПоискаВРег) Тогда
			ИмяВРег = ВРег(Строка(ЭлементСписка.Значение));
			СинонимВРег = ВРег(ЭлементСписка.Представление);
			Если СтрНайти(ИмяВРег, СтрокаПоискаВРег) = 0
				И СтрНайти(СинонимВРег, СтрокаПоискаВРег) = 0 Тогда
				Продолжить;
			КонецЕсли;
		КонецЕсли;
		
		СписокДляОтметки.Добавить(
			ЭлементСписка.Значение, ЭлементСписка.Представление, ЭлементСписка.Пометка, ЭлементСписка.Картинка);
		
	КонецЦикла;
	
	Возврат СписокДляОтметки;
	
КонецФункции

&НаКлиенте
Функция СтрокаФильтраРолейДляВыбора(ТекстПоля)
	
	ТекстФильтра = СокрЛП(ТекстПоля);
	
	Если ПустаяСтрока(ТекстФильтра) ИЛИ СтрНайти(ТекстФильтра, ",") > 0 Тогда
		Возврат "";
	КонецЕсли;
	
	Возврат ТекстФильтра;
	
КонецФункции

&НаКлиенте
Процедура ГруппаСтраницыПриСменеСтраницы(Элемент, ТекущаяСтраница)
	
	Если ТекущаяСтраница.Имя = "ГруппаСтраницаСравнениеРолей" Тогда
		ЗаполнитьСписокРолей();
	КонецЕсли;

КонецПроцедуры

&НаКлиенте
Процедура ВыбраннаяРольНачалоВыбора(Элемент, ДанныеВыбора, СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь;
	
	ОО = Новый ОписаниеОповещения("ПослеВыбораРолейСравнения", ЭтотОбъект);
	СтрокаФильтра = СтрокаФильтраРолейДляВыбора(Элемент.ТекстРедактирования);
	СписокДляОтметки = ПолучитьСписокРолейДляОтметкиНаСервере(СтрокаФильтра);
	Заголовок = "Выберите роли для сравнения";
	Если ЗначениеЗаполнено(СтрокаФильтра) Тогда
		Заголовок = Заголовок + " (найдено: " + Формат(СписокДляОтметки.Количество(), "ЧГ=0") + ")";
	КонецЕсли;
	
	СписокДляОтметки.ПоказатьОтметкуЭлементов(ОО, Заголовок);
	
КонецПроцедуры    

&НаКлиенте
Процедура ПослеВыбораРолейСравнения(Результат, ДополнительныеПараметры) Экспорт

	Если Результат = Неопределено Тогда
		Возврат;
	КонецЕсли;                                                                        
	
	МассивРолейСравнения = Новый Массив;
	
	Для Каждого Строка Из Результат Цикл
		
		ЭлементОсновногоСписка = СписокРолейМетаданных.НайтиПоЗначению(Строка.Значение);
		Если ЭлементОсновногоСписка <> Неопределено Тогда
			ЭлементОсновногоСписка.Пометка = Строка.Пометка;
		КонецЕсли;
		
		Если Строка.Пометка Тогда
			МассивРолейСравнения.Добавить(Строка);
		КонецЕсли;
		
	КонецЦикла;
	
	Если МассивРолейСравнения.Количество() Тогда
		Элементы.СравнитьРолиМетаданных.Доступность = Истина;
		ВыбраннаяРоль = СтрСоединить(МассивРолейСравнения, ", ");
	Иначе
		Элементы.СравнитьРолиМетаданных.Доступность = Ложь;
		ВыбраннаяРоль = "";	
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ВыбраннаяРольОчистка(Элемент, СтандартнаяОбработка)
	
	СписокРолейМетаданных.Очистить();
	ЗаполнитьСписокРолей();	
	Элементы.СравнитьРолиМетаданных.Доступность = Ложь;
	
	ИтоговаяТаблицаСравненияРолей.Очистить();
	
КонецПроцедуры      

&НаКлиенте
Процедура СравнитьРолиМетаданных(Команда)
	
	ИтоговаяТаблицаСравненияРолей.Очистить();
	СравнитьРолиМетаданныхНаСервере();       
	
	Элементы.ПоказатьВОбработке.Доступность = ?(ИтоговаяТаблицаСравненияРолей.Количество(), Истина, Ложь);	
		
КонецПроцедуры  

&НаСервере                                                             
Процедура СравнитьРолиМетаданныхНаСервере()	
	
	РолиМТД = Новый Массив;
	
	Для Каждого Строка Из СписокРолейМетаданных Цикл	
		Если Строка.Пометка Тогда
			РолиМТД.Добавить(Метаданные.Роли.Найти(Строка.Значение));
		КонецЕсли;	
	КонецЦикла;
	
	Для Каждого Строка Из РолиМТД Цикл
		
		Для Каждого ОМ Из ТаблицаКонфигурации Цикл 
			
			ТекОбъектМТД = Метаданные.НайтиПоПолномуИмени(ОМ.ПолноеИмя);
			Если ТекОбъектМТД = Неопределено Тогда
				Продолжить;
			КонецЕсли;
			
			Попытка             
				
				Если ПравоДоступа("Использование", ТекОбъектМТД, Метаданные.Роли.Найти(Строка.Имя)) Тогда	 
					
					СтрокаНайдена = ТаблицаВхождений.НайтиСтроки(Новый Структура("ОбъектМетаданныхИмя", ОМ.Имя));   
					
					Если НЕ СтрокаНайдена.Количество() Тогда 
						НоваяСтрока = ТаблицаВхождений.Добавить();
						НоваяСтрока.ОбъектМетаданныхИмя = ОМ.Имя; 
						НоваяСтрока.ОбъектМетаданныхПолноеИмя = ОМ.ПолноеИмя;
						НоваяСтрока.ПолныйСиноним = ОМ.Синоним; 
						НоваяСтрока.Картинка = ОМ.Картинка;
					КонецЕсли;
					
				КонецЕсли;
				
			Исключение
			КонецПопытки;
			
			Попытка 
				
				СтрокаНайдена = ТаблицаВхождений.НайтиСтроки(Новый Структура("ОбъектМетаданныхИмя", ОМ.Имя));   
				
				Если НЕ СтрокаНайдена.Количество() Тогда      
					
					Если ПравоДоступа("Чтение", ТекОбъектМТД, Метаданные.Роли.Найти(Строка.Имя)) Тогда
						НоваяСтрока = ТаблицаВхождений.Добавить();
						НоваяСтрока.ОбъектМетаданныхИмя = ОМ.Имя; 
						НоваяСтрока.ОбъектМетаданныхПолноеИмя = ОМ.ПолноеИмя;
						НоваяСтрока.ПолныйСиноним = ОМ.Синоним;
						НоваяСтрока.Картинка = ОМ.Картинка;
					КонецЕсли;                                                 
					
				КонецЕсли;
				
			Исключение
			КонецПопытки;
			
			Попытка 
				
				СтрокаНайдена = ТаблицаВхождений.НайтиСтроки(Новый Структура("ОбъектМетаданныхИмя", ОМ.Имя));   
				
				Если НЕ СтрокаНайдена.Количество() Тогда   
					
					Если ПравоДоступа("Просмотр", ТекОбъектМТД, Метаданные.Роли.Найти(Строка.Имя)) Тогда
						НоваяСтрока = ТаблицаВхождений.Добавить();
						НоваяСтрока.ОбъектМетаданныхИмя = ОМ.Имя; 
						НоваяСтрока.ОбъектМетаданныхПолноеИмя = ОМ.ПолноеИмя;
						НоваяСтрока.ПолныйСиноним = ОМ.Синоним;  
						НоваяСтрока.Картинка = ОМ.Картинка;
					КонецЕсли;                                             
					
				КонецЕсли;
				
			Исключение
			КонецПопытки; 
			
		КонецЦикла;
		
		НоваяСтрока = ИтоговаяТаблицаСравненияРолей.Добавить();
		НоваяСтрока.Имя = Строка.Имя;
		НоваяСтрока.Картинка = БиблиотекаКартинок.ЗакрытыйКлюч;
		НоваяСтрока.ЭтоРоль = Истина;
		
		Для Каждого СтрокаОМ Из ТаблицаВхождений Цикл
			
			НоваяСтрока = ИтоговаяТаблицаСравненияРолей.Добавить();
			НоваяСтрока.Имя = СтрокаОМ.ОбъектМетаданныхПолноеИмя;
			НоваяСтрока.Картинка = СтрокаОМ.Картинка;
			
		КонецЦикла;
		
		ТаблицаВхождений.Очистить();
		
	КонецЦикла;  
	
	Для Каждого Строка Из ИтоговаяТаблицаСравненияРолей Цикл
		
		Если Строка.ЭтоРоль Тогда
			МетаРоль = Метаданные.Роли.Найти(Строка.Имя);
			Продолжить;	                          
		КонецЕсли;
		
		НайденныйОбъект = Метаданные.НайтиПоПолномуИмени(Строка.Имя); 
				
		НачалоТочки = СтрНайти(Строка.Имя, ".") -1;
		ВидОбъекта = Сред(Строка.Имя, 0, НачалоТочки);
		
		Если ВидОбъекта = "Обработка" ИЛИ ВидОбъекта = "Отчет" Тогда
			
			Если ПравоДоступа("Использование", НайденныйОбъект, МетаРоль) Тогда				
				
				Строка.Использование = Истина;
				
				Если ПравоДоступа("Просмотр", НайденныйОбъект, МетаРоль) Тогда
					Строка.Просмотр = Истина;	
				КонецЕсли;
				
			КонецЕсли;
			
		ИначеЕсли ВидОбъекта = "Справочник" ИЛИ ВидОбъекта = "ПланВидовХарактеристик" ИЛИ ВидОбъекта = "ПланСчетов" ИЛИ ВидОбъекта = "ПланВидовРасчета" Тогда
			
			
			Если ПравоДоступа("Чтение", НайденныйОбъект, МетаРоль) Тогда
				
				Строка.Чтение = Истина;
				
				Если ПравоДоступа("Изменение", НайденныйОбъект, МетаРоль) Тогда
					Строка.Изменение = Истина;	
				КонецЕсли; 
				Если ПравоДоступа("Добавление", НайденныйОбъект, МетаРоль) Тогда
					Строка.Добавление = Истина;	
				КонецЕсли;
				Если ПравоДоступа("Просмотр", НайденныйОбъект, МетаРоль) Тогда
					Строка.Просмотр = Истина;	
				КонецЕсли;
				Если ПравоДоступа("Редактирование", НайденныйОбъект, МетаРоль) Тогда
					Строка.Редактирование = Истина;	
				КонецЕсли;
				Если ПравоДоступа("Удаление", НайденныйОбъект, МетаРоль) Тогда
					Строка.Удаление = Истина;
				КонецЕсли;
				
			КонецЕсли;
			
		ИначеЕсли ВидОбъекта = "Документ" Тогда	
			
			Если ПравоДоступа("Чтение", НайденныйОбъект, МетаРоль) Тогда				
				
				Строка.Чтение = Истина;
				
				Если ПравоДоступа("Изменение", НайденныйОбъект, МетаРоль) Тогда
					Строка.Изменение = Истина;	
				КонецЕсли; 
				Если ПравоДоступа("Добавление", НайденныйОбъект, МетаРоль) Тогда
					Строка.Добавление = Истина;	
				КонецЕсли;
				Если ПравоДоступа("Просмотр", НайденныйОбъект, МетаРоль) Тогда
					Строка.Просмотр = Истина;	
				КонецЕсли;
				Если ПравоДоступа("Редактирование", НайденныйОбъект, МетаРоль) Тогда
					Строка.Редактирование = Истина;	
				КонецЕсли;
				Если ПравоДоступа("Проведение", НайденныйОбъект, МетаРоль) Тогда
					Строка.Проведение = Истина;	
				КонецЕсли;
				Если ПравоДоступа("Удаление", НайденныйОбъект, МетаРоль) Тогда
					Строка.Удаление = Истина;
				КонецЕсли;
				
			КонецЕсли;
			
		ИначеЕсли ВидОбъекта = "Константа" ИЛИ ВидОбъекта = "РегистрРасчета" Тогда
			
			Если ПравоДоступа("Чтение", НайденныйОбъект, МетаРоль) Тогда				
				
				Строка.Чтение = Истина;
				
				Если ПравоДоступа("Изменение", НайденныйОбъект, МетаРоль) Тогда
					Строка.Изменение = Истина;	
				КонецЕсли; 
				Если ПравоДоступа("Просмотр", НайденныйОбъект, МетаРоль) Тогда
					Строка.Просмотр = Истина;	
				КонецЕсли;
				Если ПравоДоступа("Редактирование", НайденныйОбъект, МетаРоль) Тогда
					Строка.Редактирование = Истина;	
				КонецЕсли;
				
			КонецЕсли;
			
		ИначеЕсли ВидОбъекта = "ЖурналДокументов" Тогда
			
			Если ПравоДоступа("Чтение", НайденныйОбъект, МетаРоль) Тогда				
				
				Строка.Чтение = Истина;
				
				Если ПравоДоступа("Просмотр", НайденныйОбъект, МетаРоль) Тогда
					Строка.Просмотр = Истина;	
				КонецЕсли;
				
			КонецЕсли;
			
		ИначеЕсли ВидОбъекта = "РегистрСведений" ИЛИ ВидОбъекта = "РегистрНакопления" ИЛИ ВидОбъекта = "РегистрБухгалтерии" Тогда	
			
			Если ПравоДоступа("Чтение", НайденныйОбъект, МетаРоль) Тогда				
				
				Строка.Чтение = Истина;
				
				Если ПравоДоступа("Изменение", НайденныйОбъект, МетаРоль) Тогда
					Строка.Изменение = Истина;	
				КонецЕсли; 
				Если ПравоДоступа("Просмотр", НайденныйОбъект, МетаРоль) Тогда
					Строка.Просмотр = Истина;	
				КонецЕсли;
				Если ПравоДоступа("Редактирование", НайденныйОбъект, МетаРоль) Тогда
					Строка.Редактирование = Истина;	
				КонецЕсли;
				Если ПравоДоступа("УправлениеИтогами", НайденныйОбъект, МетаРоль) Тогда
					Строка.УправлениеИтогами = Истина;	
				КонецЕсли;
				
			КонецЕсли;
			
		ИначеЕсли ВидОбъекта = "Подсистема" Тогда
			
			Если ПравоДоступа("Просмотр", НайденныйОбъект, МетаРоль) Тогда				
				Строка.Просмотр = Истина;		
			КонецЕсли;
			
		КонецЕсли;		
		
	КонецЦикла;
	
	ТЗКопия = РеквизитФормыВЗначение("ИтоговаяТаблицаСравненияРолей");

	ОписаниеТипаЧисло = Новый ОписаниеТипов("Число");
	
	АгрегированныеРоли = Новый ТаблицаЗначений;
	АгрегированныеРоли.Колонки.Добавить("Имя", Новый ОписаниеТипов("Строка"));
	АгрегированныеРоли.Колонки.Добавить("Чтение", ОписаниеТипаЧисло);
	АгрегированныеРоли.Колонки.Добавить("Изменение", ОписаниеТипаЧисло);
	АгрегированныеРоли.Колонки.Добавить("Добавление", ОписаниеТипаЧисло);
	АгрегированныеРоли.Колонки.Добавить("Удаление", ОписаниеТипаЧисло);
	АгрегированныеРоли.Колонки.Добавить("Просмотр", ОписаниеТипаЧисло);
	АгрегированныеРоли.Колонки.Добавить("Редактирование", ОписаниеТипаЧисло);
	АгрегированныеРоли.Колонки.Добавить("Проведение", ОписаниеТипаЧисло);
	АгрегированныеРоли.Колонки.Добавить("Использование", ОписаниеТипаЧисло);
	АгрегированныеРоли.Колонки.Добавить("УправлениеИтогами", ОписаниеТипаЧисло);
	АгрегированныеРоли.Колонки.Добавить("Картинка", Новый ОписаниеТипов("Картинка"));
	АгрегированныеРоли.Колонки.Добавить("ЭтоРоль", ОписаниеТипаЧисло);
	
	НоваяСтрока = ИтоговаяТаблицаСравненияРолей.Добавить();
	НоваяСтрока.Имя = "Итоговые показатели доступа при сложении ролей";
	НоваяСтрока.ЭтоРоль = Истина; 
	НоваяСтрока.Картинка = БиблиотекаКартинок.ПроизвольноеВыражение;
	
	ОдиночныеРоли = ТЗКопия.СкопироватьКолонки();
	
	Для Каждого Строка Из ИтоговаяТаблицаСравненияРолей Цикл
		
		Если Строка.ЭтоРоль Тогда
			Продолжить;
		КонецЕсли;
		
		СтрокиРолей = ИтоговаяТаблицаСравненияРолей.НайтиСтроки(Новый Структура("Имя", Строка.Имя));
		
		Если СтрокиРолей.Количество() = 1 Тогда
			
			ЗаполнитьЗначенияСвойств(ОдиночныеРоли.Добавить(), Строка);
	
		ИначеЕсли СтрокиРолей.Количество() > 1 Тогда
			
			Для Каждого Строка Из СтрокиРолей Цикл	
				ЗаполнитьЗначенияСвойств(АгрегированныеРоли.Добавить(), Строка);				
			КонецЦикла;
					
		КонецЕсли;
		
		Если АгрегированныеРоли.Количество() Тогда
			
			АгрегированныеРоли.Свернуть("Имя, Картинка", "Чтение, Изменение, Добавление, Удаление, Просмотр, Редактирование, Проведение, Использование, УправлениеИтогами");
			
		КонецЕсли;
			
	КонецЦикла; 
	
	Для Каждого Строка Из ОдиночныеРоли Цикл
		ЗаполнитьЗначенияСвойств(ИтоговаяТаблицаСравненияРолей.Добавить(), Строка);	
	КонецЦикла;                            
	
	Для Каждого Строка Из АгрегированныеРоли Цикл
		ЗаполнитьЗначенияСвойств(ИтоговаяТаблицаСравненияРолей.Добавить(), Строка);		
	КонецЦикла;  
			
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьВОбработке(Команда)
	
	ТекДанные = Элементы.ИтоговаяТаблицаСравненияРолей.ТекущиеДанные;
	Если ТекДанные = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	Оповестить("ПоказатьОбъектВДереве", Новый Структура("ТекОбъект", ТекДанные.Имя));
	
КонецПроцедуры

&НаКлиенте
Процедура СравнениеГруппПредставлениеОткрытие(Элемент, СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь;
	
	ПараметрыОткрытия = Новый Структура("Ключ", Элементы.СравнениеГрупп.ТекущиеДанные.ГруппаДоступа);
	ОткрытьФорму("Справочник.ГруппыДоступа.Форма.ФормаЭлемента", ПараметрыОткрытия, Элемент,,,,, РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
	
КонецПроцедуры

&НаКлиенте
Процедура СравнениеПрофилейПредставлениеОткрытие(Элемент, СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь;
	
	ПараметрыОткрытия = Новый Структура("Ключ", Элементы.СравнениеПрофилей.ТекущиеДанные.Профиль);
	ОткрытьФорму("Справочник.ПрофилиГруппДоступа.Форма.ФормаЭлемента", ПараметрыОткрытия, Элемент,,,,, РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьСписокПрофилей(Команда)
	
	Если Элементы.СравнениеРолей.ТекущиеДанные = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	ПараметрыОткрытия = Новый Структура("ОбъектПроверки", Элементы.СравнениеРолей.ТекущиеДанные.Роль);
	ОткрытьФорму("ВнешняяОбработка.АнализИКонтрольДоступа.Форма.ФормаПоказатьПользователей", ПараметрыОткрытия, ЭтотОбъект,,,,, РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
	
КонецПроцедуры

#КонецОбласти
