
#Область ОписаниеПеременных

#КонецОбласти

#Область ПрограммныйИнтерфейс

Процедура ПроверитьВозможностьЗапуска(Отказ, Объект, МассивОшибок) Экспорт
	
	Если Метаданные.ОбщиеМодули.Найти("СтандартныеПодсистемыСервер") = Неопределено Тогда
		МассивОшибок.Добавить("БСП не обнаружена, работа программы прекращена");
		Отказ = Истина;
	КонецЕсли;
	
	ВерсияБСП = СтандартныеПодсистемыСервер.ВерсияБиблиотеки();
	
	Если Число(Лев(ВерсияБСП, 1)) < 3 Тогда
		МассивОшибок.Добавить("Текущая версия БСП (" + Лев(ВерсияБСП, 3) + ") не поддерживается обработкой, требуется от (3.1) и выше.");
		Отказ = Истина;
		Возврат;
	КонецЕсли;
	
КонецПроцедуры

Функция ИмяОтложенногоОпределенияКонфигурации() Экспорт
	
	Возврат "ПриЗапуске";
	
КонецФункции

Функция ЭтоОтложенноеОпределениеКонфигурации(ИмяКонфигурации) Экспорт
	
	Возврат ИмяКонфигурации = ИмяОтложенногоОпределенияКонфигурации();
	
КонецФункции

Функция ТребуетсяВыборКонфигурацииПриЗапуске(ИмяКонфигурации) Экспорт
	
	Возврат НЕ ЗначениеЗаполнено(ИмяКонфигурации)
		ИЛИ ЭтоОтложенноеОпределениеКонфигурации(ИмяКонфигурации);
	
КонецФункции

Функция ПолучитьСписокПоддерживаемыхКонфигураций() Экспорт
	
	Список = Новый СписокЗначений;
	Список.Добавить("УТ", "1С:Управление торговлей");
	Список.Добавить("КА", "1С:Комплексная автоматизация");
	Список.Добавить("ERP", "1С:ERP Управление предприятием");
	Список.Добавить("БП", "1С:Бухгалтерия предприятия");
	Список.Добавить("ЗУП", "1С:Зарплата и управление персоналом");
	Список.Добавить("УНФ", "1С:Управление нашей фирмой");
	Список.Добавить("ДО", "1С:Документооборот 3.0");
	Список.Добавить("Другая", "Другая конфигурация");
	
	Возврат Список;
	
КонецФункции

Функция ПолучитьСписокКонфигурацийДляВыбора() Экспорт
	
	Список = ПолучитьСписокПоддерживаемыхКонфигураций();
	Список.Добавить(ИмяОтложенногоОпределенияКонфигурации(), "Определить при следующем запуске");
	
	Возврат Список;
	
КонецФункции

Функция ЭффективноеИмяКонфигурации(ИмяКонфигурации, ОпределеннаяКонфигурация = "") Экспорт
	
	Если ЭтоОтложенноеОпределениеКонфигурации(ИмяКонфигурации)
		ИЛИ НЕ ЗначениеЗаполнено(ИмяКонфигурации) Тогда
		
		Если ЗначениеЗаполнено(ОпределеннаяКонфигурация) Тогда
			Возврат ОпределеннаяКонфигурация;
		КонецЕсли;
		
		Возврат "Другая";
		
	КонецЕсли;
	
	Возврат ИмяКонфигурации;
	
КонецФункции

Функция ОпределитьКонфигурацию() Экспорт
	
	Синоним = ВРег(Метаданные.Синоним);
	Имя = ВРег(Метаданные.Имя);
	
	Если СтрНайти(Синоним, "УПРАВЛЕНИЕ ТОРГОВЛЕЙ") > 0 Тогда
		Возврат "УТ";
	ИначеЕсли СтрНайти(Синоним, "КОМПЛЕКСНАЯ АВТОМАТИЗАЦИЯ") > 0 Тогда
		Возврат "КА";
	ИначеЕсли СтрНайти(Синоним, "ERP") > 0 ИЛИ СтрНайти(Имя, "ERP") > 0 Тогда
		Возврат "ERP";
	ИначеЕсли СтрНайти(Синоним, "БУХГАЛТЕРИЯ ПРЕДПРИЯТИЯ") > 0 Тогда
		Возврат "БП";
	ИначеЕсли СтрНайти(Синоним, "ЗАРПЛАТА И УПРАВЛЕНИЕ ПЕРСОНАЛОМ") > 0
		ИЛИ СтрНайти(Синоним, "ЗУП") > 0 Тогда
		Возврат "ЗУП";
	ИначеЕсли СтрНайти(Синоним, "УПРАВЛЕНИЕ НАШЕЙ ФИРМОЙ") > 0 Тогда
		Возврат "УНФ";
	ИначеЕсли СтрНайти(Синоним, "ДОКУМЕНТООБОРОТ") > 0 Тогда
		Возврат "ДО";
	КонецЕсли;
	
	Возврат "";
	
КонецФункции

Функция ПредставлениеКонфигурации(ИмяКонфигурации) Экспорт
	
	ЭлементСписка = ПолучитьСписокКонфигурацийДляВыбора().НайтиПоЗначению(ИмяКонфигурации);
	
	Если ЭлементСписка <> Неопределено Тогда
		Возврат ЭлементСписка.Представление;
	КонецЕсли;
	
	Возврат ИмяКонфигурации;
	
КонецФункции

Функция КонфигурацияПоддерживаетАналитикуСотрудников(ИмяКонфигурации) Экспорт
	
	Возврат ИмяКонфигурации = "КА"
		ИЛИ ИмяКонфигурации = "ERP"
		ИЛИ ИмяКонфигурации = "ЗУП";
	
КонецФункции

Функция КонфигурацияПоддерживаетПодборГруппПоОрганизации(ИмяКонфигурации) Экспорт
	
	Возврат КонфигурацияПоддерживаетАналитикуСотрудников(ИмяКонфигурации);
	
КонецФункции

// Префикс ключей настроек обработки во всех хранилищах ИБ.
// Пример: ВнешняяОбработка.АнализИКонтрольДоступа.Форма.ФормаПодбораРолей/...
Функция ПрефиксКлючаНастроекОбработки() Экспорт
	
	Возврат "ВнешняяОбработка." + Метаданные().Имя;
	
КонецФункции

// Начальные значения настроек при первом открытии (или после сброса).
Функция ПолучитьНастройкиПоУмолчанию() Экспорт
	
	ТекДата = ТекущаяДатаСеанса();
	ПериодПоиска = Новый СтандартныйПериод(НачалоДня(ТекДата), ТекДата);
	
	ТекПользовательИБ = ПользователиИнформационнойБазы.ТекущийПользователь();
	ОтключитьПредупреждение = НЕ ТекПользовательИБ.ЗащитаОтОпасныхДействий.ПредупреждатьОбОпасныхДействиях;
	
	Настройки = Новый Структура;
	Настройки.Вставить("ПроверятьНаличиеОбновлений", Истина);
	Настройки.Вставить("ФормироватьОтчетыПоРолямВФоне", Истина);
	Настройки.Вставить("ОтключитьПредупреждениеОбОпасныхДействиях", ОтключитьПредупреждение);
	Настройки.Вставить("ИспользоватьАвтоПоискОшибок", Ложь);
	Настройки.Вставить("УведомлятьОбОшибкахПриОткрытии", Истина);
	Настройки.Вставить("ПоказыватьОрганизациюИДолжность", Ложь);
	Настройки.Вставить("ИспользуетсяТерминальныйРежим", Ложь);
	Настройки.Вставить("ДинамическийПериодПоиска", Ложь);
	Настройки.Вставить("ПутьДоПользователей", "");
	Настройки.Вставить("ИмяКонфигурации", "");
	Настройки.Вставить("ПериодПоискаОшибок", ПериодПоиска);
	Настройки.Вставить("КоличествоДнейНазадДляПоискаОшибки", 0);
	
	Возврат Настройки;
	
КонецФункции

// Дополняет переданную структуру настроек значениями по умолчанию для отсутствующих ключей.
Функция ДополнитьНастройкиЗначениямиПоУмолчанию(Настройки) Экспорт
	
	Для Каждого Элемент Из ПолучитьНастройкиПоУмолчанию() Цикл
		
		Если Настройки.Получить(Элемент.Ключ) = Неопределено Тогда
			Настройки.Вставить(Элемент.Ключ, Элемент.Значение);
		КонецЕсли;
		
	КонецЦикла;
	
	Возврат Настройки;
	
КонецФункции

// Удаляет все записи обработки из хранилищ настроек текущего пользователя ИБ.
Функция ОчиститьНастройкиОбработкиВХранилищах(ИмяПользователяИБ = "") Экспорт
	
	Если НЕ ЗначениеЗаполнено(ИмяПользователяИБ) Тогда
		ИмяПользователяИБ = ПользователиИнформационнойБазы.ТекущийПользователь().Имя;
	КонецЕсли;
	
	ПрефиксКлюча = ПрефиксКлючаНастроекОбработки();
	КоличествоУдаленных = 0;
	
	Для Каждого Хранилище Из ПолучитьХранилищаНастроекДляОчистки() Цикл
		КоличествоУдаленных = КоличествоУдаленных
			+ УдалитьНастройкиОбработкиИзХранилища(Хранилище, ИмяПользователяИБ, ПрефиксКлюча);
	КонецЦикла;
	
	Возврат КоличествоУдаленных;
	
КонецФункции

Функция ПолучитьХранилищаНастроекДляОчистки()
	
	ИменаХранилищ = Новый Массив;
	ИменаХранилищ.Добавить("ХранилищеНастроекДанныхФорм");
	ИменаХранилищ.Добавить("ХранилищеОбщихНастроек");
	ИменаХранилищ.Добавить("ХранилищеПользовательскихНастроекДинамическихСписков");
	ИменаХранилищ.Добавить("ХранилищеПользовательскихНастроекОтчетов");
	ИменаХранилищ.Добавить("ХранилищеСистемныхНастроек");
	ИменаХранилищ.Добавить("ХранилищеВариантовОтчетов");
	
	Хранилища = Новый Массив;
	
	Для Каждого ИмяХранилища Из ИменаХранилищ Цикл
		
		Попытка
			Хранилище = Вычислить(ИмяХранилища);
		Исключение
			Продолжить;
		КонецПопытки;
		
		Если Хранилище <> Неопределено Тогда
			Хранилища.Добавить(Хранилище);
		КонецЕсли;
		
	КонецЦикла;
	
	Возврат Хранилища;
	
КонецФункции

Функция УдалитьНастройкиОбработкиИзХранилища(Хранилище, ИмяПользователяИБ, ПрефиксКлюча)
	
	КоличествоУдаленных = 0;
	Отбор = Новый Структура("Пользователь", ИмяПользователяИБ);
	
	Попытка
		Выборка = Хранилище.Выбрать(Отбор);
	Исключение
		Возврат 0;
	КонецПопытки;
	
	Пока Выборка.Следующий() Цикл
		
		Если НЕ ЭтоКлючНастройкиОбработки(Выборка.КлючОбъекта, ПрефиксКлюча) Тогда
			Продолжить;
		КонецЕсли;
		
		Хранилище.Удалить(Выборка.КлючОбъекта, Выборка.КлючНастроек, Выборка.Пользователь);
		КоличествоУдаленных = КоличествоУдаленных + 1;
		
	КонецЦикла;
	
	Возврат КоличествоУдаленных;
	
КонецФункции

Функция ЭтоКлючНастройкиОбработки(КлючОбъекта, ПрефиксКлюча)
	
	Возврат ЗначениеЗаполнено(КлючОбъекта)
		И СтрНачинаетсяС(КлючОбъекта, ПрефиксКлюча);
	
КонецФункции

Функция ПолучитьОписаниеКоллекцийМетаданных() Экспорт
	
	Коллекции = Новый Массив;
	
	// Группа «Общие» — порядок как в конфигураторе (вкладка «Права» роли).
	ДобавитьОписаниеКоллекцииМетаданных(Коллекции, "Подсистемы", "Подсистемы", Истина, 1, "Подсистема", "Подсистема");
	ДобавитьОписаниеКоллекцииМетаданных(Коллекции, "ПараметрыСеанса", "Параметры сеанса", Истина, 2, "ПараметрСеанса", "ПараметрСеанса");
	ДобавитьОписаниеКоллекцииМетаданных(Коллекции, "ОбщиеРеквизиты", "Общие реквизиты", Истина, 3, "ОбщийРеквизит", "ОбщийРеквизит");
	ДобавитьОписаниеКоллекцииМетаданных(Коллекции, "ПланыОбмена", "Планы обмена", Истина, 4, "ПланОбмена", "ПланОбмена");
	ДобавитьОписаниеКоллекцииМетаданных(Коллекции, "КритерииОтбора", "Критерии отбора", Истина, 5, "КритерийОтбора", "КритерийОтбора");
	ДобавитьОписаниеКоллекцииМетаданных(Коллекции, "ОбщиеКоманды", "Общие команды", Истина, 6, "ОбщаяКоманда", "ОбщаяКоманда");
	ДобавитьОписаниеКоллекцииМетаданных(Коллекции, "ОбщиеФормы", "Общие формы", Истина, 7, "Форма", "ОбщаяФорма");
	ДобавитьОписаниеКоллекцииМетаданных(Коллекции, "WebСервисы", "Web-сервисы", Истина, 8, "WebСервис", "WebСервис");
	ДобавитьОписаниеКоллекцииМетаданных(Коллекции, "HTTPСервисы", "HTTP-сервисы", Истина, 9, "HTTPСервис", "HTTPСервис");
	ДобавитьОписаниеКоллекцииМетаданных(Коллекции, "СервисыИнтеграции", "Сервисы интеграции", Истина, 10, "СервисИнтеграции", "СервисИнтеграции");
	
	// Корневые коллекции конфигурации.
	ДобавитьОписаниеКоллекцииМетаданных(Коллекции, "Константы", "Константы", Ложь, 11, "Константа", "Константа");
	ДобавитьОписаниеКоллекцииМетаданных(Коллекции, "Справочники", "Справочники", Ложь, 12, "Справочник", "Справочник");
	ДобавитьОписаниеКоллекцииМетаданных(Коллекции, "Документы", "Документы", Ложь, 13, "Документ", "Документ");
	ДобавитьОписаниеКоллекцииМетаданных(Коллекции, "ЖурналыДокументов", "Журналы документов", Ложь, 14, "ЖурналДокументов", "ЖурналДокументов");
	ДобавитьОписаниеКоллекцииМетаданных(Коллекции, "Отчеты", "Отчеты", Ложь, 16, "Отчет", "Отчет");
	ДобавитьОписаниеКоллекцииМетаданных(Коллекции, "Обработки", "Обработки", Ложь, 17, "Обработка", "Обработка");
	ДобавитьОписаниеКоллекцииМетаданных(Коллекции, "ПланыВидовХарактеристик", "Планы видов характеристик", Ложь, 18, "ПланВидовХарактеристик", "ПланВидовХарактеристик");
	ДобавитьОписаниеКоллекцииМетаданных(Коллекции, "ПланыСчетов", "Планы счетов", Ложь, 19, "ПланСчетов", "ПланСчетов");
	ДобавитьОписаниеКоллекцииМетаданных(Коллекции, "ПланыВидовРасчета", "Планы видов расчета", Ложь, 20, "ПланВидовРасчета", "ПланВидовРасчета");
	ДобавитьОписаниеКоллекцииМетаданных(Коллекции, "РегистрыСведений", "Регистры сведений", Ложь, 21, "РегистрСведений", "РегистрСведений");
	ДобавитьОписаниеКоллекцииМетаданных(Коллекции, "РегистрыНакопления", "Регистры накопления", Ложь, 22, "РегистрНакопления", "РегистрНакопления");
	ДобавитьОписаниеКоллекцииМетаданных(Коллекции, "РегистрыБухгалтерии", "Регистры бухгалтерии", Ложь, 23, "РегистрБухгалтерии", "РегистрБухгалтерии");
	ДобавитьОписаниеКоллекцииМетаданных(Коллекции, "РегистрыРасчета", "Регистры расчета", Ложь, 24, "РегистрРасчета", "РегистрРасчета");
	ДобавитьОписаниеКоллекцииМетаданных(Коллекции, "БизнесПроцессы", "Бизнес-процессы", Ложь, 25, "БизнесПроцесс", "БизнесПроцесс");
	ДобавитьОписаниеКоллекцииМетаданных(Коллекции, "Задачи", "Задачи", Ложь, 26, "Задача", "Задача");
	ДобавитьОписаниеКоллекцииМетаданных(Коллекции, "ВнешниеИсточникиДанных", "Внешние источники данных", Ложь, 27, "ВнешнийИсточникДанных", "ВнешнийИсточникДанных");
	
	Возврат Коллекции;
	
КонецФункции

Функция КартинкаКоллекцииМетаданных(ИмяКартинки) Экспорт
	
	Для Каждого Имя Из ИменаКартинокМетаданныхДляПоиска(ИмяКартинки) Цикл
		
		Попытка
			Возврат БиблиотекаКартинок[Имя];
		Исключение
		КонецПопытки;
		
	КонецЦикла;
	
	Возврат БиблиотекаКартинок.Документ;
	
КонецФункции

Процедура ЗаполнитьСписокКоллекцийМетаданных(СписокМетаданных, КартинкиМетаданных = Неопределено) Экспорт
	
	СписокМетаданных.Очистить();
	
	Если КартинкиМетаданных <> Неопределено Тогда
		КартинкиМетаданных.Очистить();
	КонецЕсли;
	
	Для Каждого ОписаниеКоллекции Из ПолучитьОписаниеКоллекцийМетаданных() Цикл
		
		СписокМетаданных.Добавить(
			ОписаниеКоллекции.Имя,
			ОписаниеКоллекции.Синоним,
			Истина,
			ОписаниеКоллекции.Картинка);
		
		Если КартинкиМетаданных <> Неопределено Тогда
			КартинкиМетаданных.Добавить(
				ОписаниеКоллекции.ИмяТипаОбъекта,
				ОписаниеКоллекции.Синоним,
				Истина,
				ОписаниеКоллекции.Картинка);
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры

Процедура ЗаполнитьТаблицуОбъектовМетаданных(ТаблицаКонфигурации) Экспорт
	
	Для Каждого ОписаниеКоллекции Из ПолучитьОписаниеКоллекцийМетаданных() Цикл
		
		Для Каждого ОбъектМетаданных Из Метаданные[ОписаниеКоллекции.Имя] Цикл
			
			НоваяСтрока = ТаблицаКонфигурации.Добавить();
			НоваяСтрока.Имя = ОбъектМетаданных.Имя;
			НоваяСтрока.ПолноеИмя = ОбъектМетаданных.ПолноеИмя();
			НоваяСтрока.Синоним = ОбъектМетаданных.Синоним;
			НоваяСтрока.Картинка = ОписаниеКоллекции.Картинка;
			
		КонецЦикла;
		
	КонецЦикла;
	
КонецПроцедуры

Функция ПолучитьЖурналРегистрации(ДатаНачала, ДатаОкончания, ЭтоУведомление = Ложь) Экспорт
	
	тзЖурнал = Новый ТаблицаЗначений;
	
	Отбор = Новый Структура;
	Отбор.Вставить("ДатаНачала", ДатаНачала);
	Отбор.Вставить("ДатаОкончания", ?(ДатаОкончания = Неопределено, ТекущаяДатаСеанса(), ДатаОкончания));
	
	Если ЭтоУведомление Тогда
		ПометкиТаблицыУровней = ПолучитьТипыОшибок();
		
		УровниЖурнала = Новый Массив;
		Если ПометкиТаблицыУровней.Первая = "Истина" Тогда
			УровниЖурнала.Добавить(УровеньЖурналаРегистрации.Информация);
		КонецЕсли;
		
		Если ПометкиТаблицыУровней.Вторая = "Истина" Тогда
			УровниЖурнала.Добавить(УровеньЖурналаРегистрации.Ошибка);
		КонецЕсли;
	КонецЕсли;
	
	СписокУровней = Новый Массив;
	Если УровниЖурнала = Неопределено Тогда
		СписокУровней.Добавить(УровеньЖурналаРегистрации.Ошибка);
		СписокУровней.Добавить(УровеньЖурналаРегистрации.Информация);
	Иначе
		ОбщегоНазначенияКлиентСервер.ДополнитьМассив(СписокУровней, УровниЖурнала);
	КонецЕсли;
	
	Отбор.Вставить("Уровень", СписокУровней);
	
	СписокСобытий = Новый Массив;
	СписокСобытий.Добавить("_$PerformError$_");
	СписокСобытий.Добавить("_$Access$_.AccessDenied");
	
	Отбор.Вставить("Событие", СписокСобытий);
	
	Колонки = "ПредставлениеДанных,Данные,Комментарий,ПредставлениеМетаданных,Метаданные,Компьютер,ИмяПользователя,Пользователь,ПредставлениеСобытия,Событие,ПредставлениеПриложения,Дата";
	
	ВыгрузитьЖурналРегистрации(тзЖурнал, Отбор, Колонки);
	
	ПодложкаЖурнала = СоздатьПодложкуДляЖурнала(Колонки);
	
	Для Каждого Строка Из ТзЖурнал Цикл
		НоваяСтрока = ПодложкаЖурнала.Добавить();
		ЗаполнитьЗначенияСвойств(НоваяСтрока, Строка); 
	КонецЦикла;
	
	Запрос = Новый Запрос;
	Запрос.Текст =
	"ВЫБРАТЬ
	|	Таблица.ПредставлениеДанных КАК ПредставлениеДанных,
	|	Таблица.Комментарий КАК Комментарий,
	|	Таблица.Компьютер КАК Компьютер,
	|	Таблица.ИмяПользователя КАК ИмяПользователя,
	|	Таблица.Пользователь КАК Пользователь,
	|	Таблица.ПредставлениеСобытия КАК ПредставлениеСобытия,
	|	Таблица.Событие КАК Событие,
	|	Таблица.ПредставлениеПриложения КАК ПредставлениеПриложения,
	|	Таблица.Дата КАК Дата
	|ПОМЕСТИТЬ втТаблицаОшибок
	|ИЗ
	|	&Таблица КАК Таблица
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	втТаблицаОшибок.ПредставлениеДанных КАК ПредставлениеДанных,
	|	втТаблицаОшибок.Комментарий КАК Комментарий,
	|	втТаблицаОшибок.Компьютер КАК Компьютер,
	|	втТаблицаОшибок.ИмяПользователя КАК ИмяПользователя,
	|	втТаблицаОшибок.Пользователь КАК Пользователь,
	|	втТаблицаОшибок.ПредставлениеСобытия КАК ПредставлениеСобытия,
	|	втТаблицаОшибок.Событие КАК Событие,
	|	втТаблицаОшибок.ПредставлениеПриложения КАК ПредставлениеПриложения,
	|	втТаблицаОшибок.Дата КАК Дата,
	|	ИСТИНА КАК ЗаписьНеИсправлена
	|ПОМЕСТИТЬ втИтог
	|ИЗ
	|	втТаблицаОшибок КАК втТаблицаОшибок
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	втИтог.ПредставлениеДанных КАК ПредставлениеДанных,
	|	втИтог.Комментарий КАК Комментарий,
	|	втИтог.Компьютер КАК Компьютер,
	|	втИтог.ИмяПользователя КАК ИмяПользователя,
	|	втИтог.Пользователь КАК Пользователь,
	|	втИтог.ПредставлениеСобытия КАК ПредставлениеСобытия,
	|	втИтог.Событие КАК Событие,
	|	втИтог.ПредставлениеПриложения КАК ПредставлениеПриложения,
	|	втИтог.Дата КАК Дата,
	|	втИтог.ЗаписьНеИсправлена КАК ЗаписьНеИсправлена
	|ИЗ
	|	втИтог КАК втИтог
	|ГДЕ
	|	втИтог.ЗаписьНеИсправлена";
	Запрос.УстановитьПараметр("Таблица", ПодложкаЖурнала);
	
	ТаблицаОшибокИтог = Запрос.Выполнить().Выгрузить();
	ТаблицаОшибокИтог.Колонки.Добавить("Данные");
	ТаблицаОшибокИтог.Колонки.Добавить("Метаданные");
	ТаблицаОшибокИтог.Колонки.Добавить("ПредставлениеМетаданных");
	Для Каждого Строка Из ТаблицаОшибокИтог Цикл
		УИД = Новый УникальныйИдентификатор(Строка.Пользователь);
		
		СтруктураПоиска = Новый Структура("Пользователь, Дата, Событие", УИД, Строка.Дата, Строка.Событие);
		НайденнаяСтрока = тзЖурнал.НайтиСтроки(СтруктураПоиска);
		
		ЗаполнитьЗначенияСвойств(Строка, НайденнаяСтрока[0]);
		Строка.Пользователь = НайденнаяСтрока[0].Пользователь;
	КонецЦикла;
	
	Возврат ТаблицаОшибокИтог;
	
КонецФункции

Функция ПарсингЖурналаРегистрации(ТаблицаЖурнала, Знач ИсправленныеОшибки = Неопределено) Экспорт
	
	СчетчикОшибок = 0;                                                                                 
	МассивОшибок = Новый Массив;
	СоответствиеПользователей = Новый Соответствие;        
	
	МассУдаляемыхСтрок = Новый Массив;
	Для Каждого Строка Из ТаблицаЖурнала Цикл
		
		Если СтрНайти(Строка.Комментарий, "Недостаточно прав для работы с таблицей") Тогда
			
			Если ИсправленныеОшибки = Неопределено Тогда
				Продолжить;
			КонецЕсли;
			
			НачальнаяПозиция = СтрНайти(Строка.Комментарий, "Недостаточно прав для работы с таблицей") + 41;
			
			ПозицияТочки = СтрНайти(Строка.Комментарий, ".",, НачальнаяПозиция);
			ПозицияКонецСтроки = СтрНайти(Строка.Комментарий, """",, ПозицияТочки);
			
			ТипМетаданного = Сред(Строка.Комментарий, НачальнаяПозиция, ПозицияТочки - НачальнаяПозиция);
			ИмяМетаданного = Сред(Строка.Комментарий, ПозицияТочки + 1, ПозицияКонецСтроки - ПозицияТочки - 1);  
			
			// Ошибка? Иногда пользователь в ЖР = <Неопределено>
			Если НЕ ЗначениеЗаполнено(Строка.Пользователь) Тогда
				Продолжить;
			КонецЕсли;
		
			ТекПользователь = ПолучитьТекущегоПользователя(Строка.Пользователь);
			
			Отбор = Новый Структура;
			Отбор.Вставить("Пользователь", ТекПользователь);
			Отбор.Вставить("ТаблицаОшибки", ТипМетаданного + "." + ИмяМетаданного);
			
			Если ИсправленныеОшибки = Неопределено Тогда
				ОшибкаБылаИсправлена = Ложь;
			Иначе
				ОшибкаБылаИсправлена = ИсправленныеОшибки.НайтиСтроки(Отбор);
			КонецЕсли;
			
			Если ОшибкаБылаИсправлена.Количество() Тогда
				Продолжить;
			КонецЕсли;
			
			СтруктураМетаданных = СформироватьСткМетаданных(ТипМетаданного, ИмяМетаданного, Строка.Комментарий, Строка.Дата, Истина, Строка.Событие);
			
			ДанныеОшибок = Новый Массив;			
			ДанныеОшибок.Добавить(СтруктураМетаданных);
			
			КлючПользователя = СоответствиеПользователей.Получить(ТекПользователь);
			
			Если КлючПользователя = Неопределено Тогда				
				СоответствиеПользователей.Вставить(ТекПользователь, ДанныеОшибок);
				СчетчикОшибок = СчетчикОшибок + 1;
			ИначеЕсли НЕ ОбъектУжеИзвестен(СтруктураМетаданных, КлючПользователя) Тогда				
				КлючПользователя.Добавить(СтруктураМетаданных);
				СчетчикОшибок = СчетчикОшибок + 1;
			КонецЕсли; 
			
		ИначеЕсли СтрНайти(Строка.Комментарий, "У пользователя недостаточно прав") Тогда  
			
			Если ИсправленныеОшибки = Неопределено Тогда
				Продолжить;
			КонецЕсли;
			
			СтруктураМетаданных = СформироватьСткМетаданных("", "", Строка.Комментарий, Строка.Дата, Ложь, Строка.Событие);
			
			ДанныеОшибок = Новый Массив;			
			ДанныеОшибок.Добавить(СтруктураМетаданных);
			
			ТекПользователь = ПолучитьТекущегоПользователя(Строка.Пользователь);
			
			КлючПользователя = СоответствиеПользователей.Получить(ТекПользователь);
			
			Если КлючПользователя = Неопределено Тогда				
				СоответствиеПользователей.Вставить(ТекПользователь, ДанныеОшибок);
				СчетчикОшибок = СчетчикОшибок + 1;
			КонецЕсли;
			
		// Условие для УТ 11.4 (отличаются записи в ЖР от КА 2.5)
		ИначеЕсли СтрНайти(Строка.Комментарий, "Нарушение прав доступа!") Тогда       
			
			Если ИсправленныеОшибки = Неопределено Тогда
				Продолжить;
			КонецЕсли;
			
			СтруктураМетаданных = СформироватьСткМетаданных("", "", Строка.Комментарий, Строка.Дата, Ложь, Строка.Событие);
			
			ДанныеОшибок = Новый Массив;
			ДанныеОшибок.Добавить(СтруктураМетаданных);
			
			ТекПользователь = ПолучитьТекущегоПользователя(Строка.Пользователь);
			
			КлючПользователя = СоответствиеПользователей.Получить(ТекПользователь);
			
			Если КлючПользователя = Неопределено Тогда
				СоответствиеПользователей.Вставить(ТекПользователь, ДанныеОшибок);
				СчетчикОшибок = СчетчикОшибок + 1;
			КонецЕсли;
			
		// Строка с ошибкой зафиксирована в КА 2.5
		ИначеЕсли СтрНайти(Строка.Комментарий, "Недостаточно прав для просмотра") Тогда  
			
			Если ИсправленныеОшибки = Неопределено Тогда
				Продолжить;
			КонецЕсли;
			
			СтруктураМетаданных = СформироватьСткМетаданных("", "", Строка.Комментарий, Строка.Дата, Ложь, Строка.Событие);
			
			ДанныеОшибок = Новый Массив;
			ДанныеОшибок.Добавить(СтруктураМетаданных);
			
			ТекПользователь = ПолучитьТекущегоПользователя(Строка.Пользователь);
			
			КлючПользователя = СоответствиеПользователей.Получить(ТекПользователь);
			
			Если КлючПользователя = Неопределено Тогда
				СоответствиеПользователей.Вставить(ТекПользователь, ДанныеОшибок);
				СчетчикОшибок = СчетчикОшибок + 1;
			КонецЕсли;
			
		КонецЕсли; 
		
		Если ИсправленныеОшибки = Неопределено Тогда
			МассУдаляемыхСтрок.Добавить(Строка);
		КонецЕсли;
		
	КонецЦикла;
	
	Для Каждого Строка Из МассУдаляемыхСтрок Цикл
		ТаблицаЖурнала.Удалить(Строка);
	КонецЦикла;
	
	Возврат Новый Структура("СоответствиеПользователей, СчетчикОшибок", СоответствиеПользователей, СчетчикОшибок);
	
КонецФункции

Процедура ИзменитьСоставГруппы(Знач ДополнительныеПараметры, ОписаниеОшибки = "") Экспорт
		
	Если НЕ РазрешеноИзменениеСоставаПользователейГруппы(ДополнительныеПараметры.ГруппаДоступа) Тогда
		Если ДополнительныеПараметры.ВключитьВГруппуДоступа Тогда
			ОписаниеОшибки =
				НСтр("ru = 'Недостаточно прав для включения пользователя в группу доступа,
				           |(не ответственный за участников группы доступа или нет прав администратора).'");
		Иначе
			ОписаниеОшибки =
				НСтр("ru = 'Недостаточно прав для исключения пользователя из группы доступа,
				           |(не ответственный за участников группы доступа или нет прав администратора).'");
		КонецЕсли;
		Возврат;
	КонецЕсли;
	
	Если НЕ ДополнительныеПараметры.ВключитьВГруппуДоступа И НЕ ПользовательВключенВГруппуДоступа(ДополнительныеПараметры.ГруппаДоступа, ДополнительныеПараметры.Пользователь) Тогда
		ОписаниеОшибки =
			НСтр("ru = 'Невозможно исключить пользователя из группы доступа,
			           |так как он включен в нее косвенно.'");
		Возврат;
	КонецЕсли;
	
	Если ОбщегоНазначения.РазделениеВключено()
	   И ДополнительныеПараметры.ГруппаДоступа = ГруппаДоступаАдминистраторы
	   И ОбщегоНазначения.ПодсистемаСуществует("СтандартныеПодсистемы.РаботаВМоделиСервиса.ПользователиВМоделиСервиса") Тогда
		
		МодульПользователиСлужебныйВМоделиСервиса = ОбщегоНазначения.ОбщийМодуль("ПользователиСлужебныйВМоделиСервиса");
		ДействияСПользователемСервиса = МодульПользователиСлужебныйВМоделиСервиса.ПолучитьДействияСПользователемСервиса();
		
		Если НЕ ДействияСПользователемСервиса.ИзменениеАдминистративногоДоступа Тогда
			ВызватьИсключение
				НСтр("ru = 'Недостаточно прав доступа для изменения состава администраторов.'");
		КонецЕсли;
	КонецЕсли;
	
	УстановитьПривилегированныйРежим(Истина);
	
	НачатьТранзакцию();
	Попытка
		Блокировка = Новый БлокировкаДанных;
		ЭлементБлокировки = Блокировка.Добавить("Справочник.ГруппыДоступа");
		ЭлементБлокировки.УстановитьЗначение("Ссылка", ДополнительныеПараметры.ГруппаДоступа);
		Блокировка.Заблокировать();
		
		ГруппаДоступаОбъект = ДополнительныеПараметры.ГруппаДоступа.ПолучитьОбъект();
		ЗаблокироватьДанныеДляРедактирования(ГруппаДоступаОбъект.Ссылка, ГруппаДоступаОбъект.ВерсияДанных);
		Если ДополнительныеПараметры.ВключитьВГруппуДоступа Тогда
			Если ГруппаДоступаОбъект.Пользователи.Найти(ДополнительныеПараметры.Пользователь, "Пользователь") = Неопределено Тогда
				ГруппаДоступаОбъект.Пользователи.Добавить().Пользователь = ДополнительныеПараметры.Пользователь; 
			ИначеЕсли ДополнительныеПараметры.ВключитьВГруппуДоступа Тогда
				ОписаниеОшибки = НСтр("ru = 'Пользователь уже включен в группу.'");
			КонецЕсли;
		Иначе
			СтрокаТЧ = ГруппаДоступаОбъект.Пользователи.Найти(ДополнительныеПараметры.Пользователь, "Пользователь");
			Если СтрокаТЧ <> Неопределено Тогда
				ГруппаДоступаОбъект.Пользователи.Удалить(СтрокаТЧ);
			КонецЕсли;
		КонецЕсли;
		
		Если ГруппаДоступаОбъект.Ссылка = ГруппаДоступаАдминистраторы Тогда
			
			Если ОбщегоНазначения.РазделениеВключено() Тогда
				ГруппаДоступаОбъект.ДополнительныеСвойства.Вставить(
					"ПарольПользователяСервиса", ПарольПользователяСервиса);
			Иначе
				УправлениеДоступомСлужебный.ПроверитьНаличиеПользователяИБВГруппеДоступаАдминистраторы(
					ГруппаДоступаОбъект.Пользователи, ОписаниеОшибки);
				
				Если ЗначениеЗаполнено(ОписаниеОшибки) Тогда
					ОтменитьТранзакцию();
					РазблокироватьДанныеДляРедактирования(ГруппаДоступаОбъект.Ссылка);
					Возврат;
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;
		
		ГруппаДоступаОбъект.Записать();
		ЗафиксироватьТранзакцию();
	Исключение
		ОтменитьТранзакцию();
		РазблокироватьДанныеДляРедактирования(ГруппаДоступаОбъект.Ссылка);
		ПарольПользователяСервиса = Неопределено;
		ВызватьИсключение;
	КонецПопытки;
	
	РазблокироватьДанныеДляРедактирования(ГруппаДоступаОбъект.Ссылка);
	Если ИмяКонфигурации <> "ДО" Тогда
		УправлениеДоступомСлужебный.ЗапуститьОбновлениеДоступа();          
	КонецЕсли;
	
КонецПроцедуры  

&НаСервере
Функция СформироватьСткМетаданных(ТипМетаданного, ИмяМетаданного, Комментарий, ДатаОшибки, МожноИсправить, Событие)
	
	СтруктураМетаданных = Новый Структура;
	СтруктураМетаданных.Вставить("ТипМетаданного", ТипМетаданного);	
	СтруктураМетаданных.Вставить("ИмяМетаданного", ИмяМетаданного);
	СтруктураМетаданных.Вставить("Комментарий",    Комментарий);
	СтруктураМетаданных.Вставить("МожноИсправить", Ложь);
	СтруктураМетаданных.Вставить("ДатаОшибки",     ДатаОшибки);
	СтруктураМетаданных.Вставить("Событие",        Событие);
	
	Возврат СтруктураМетаданных;
	
КонецФункции

Функция ВерсияОбработки() Экспорт
	
	Возврат "4.7";
	
КонецФункции

Функция URLСтраницыРелизов() Экспорт
	
	Возврат "https://github.com/VorozhbitDM/Access-control-1C/releases";
	
КонецФункции

// Проверка актуальности версии на GitHub (releases/latest).
// Возвращает структуру: Состояние, Заголовок, Подсказка, ЭтоГиперссылка, АдресПерехода.
Функция ПолучитьСтатусПроверкиОбновления() Экспорт
	
	ТекущаяВерсия = ВерсияОбработки();
	АдресРелизов = URLСтраницыРелизов();
	
	Попытка
		ДанныеРелиза = ПолучитьПоследнийРелизСGitHub();
	Исключение
		Возврат РезультатПриСбоеПроверки(ТекущаяВерсия, АдресРелизов, ОписаниеОшибки());
	КонецПопытки;
	
	Если ДанныеРелиза = Неопределено Тогда
		Возврат РезультатПриСбоеПроверки(ТекущаяВерсия, АдресРелизов, "Сервер обновлений не ответил или вернул пустой ответ.");
	КонецЕсли;
	
	ПоследняяВерсия = НормализоватьСтрокуВерсии(ДанныеРелиза.ТегВерсии);
	АдресРелиза = ДанныеРелиза.АдресРелиза;
	
	Если НЕ ЗначениеЗаполнено(ПоследняяВерсия) Тогда
		Возврат РезультатПриСбоеПроверки(ТекущаяВерсия, АдресРелизов, "Не удалось определить номер последней версии в ответе GitHub.");
	КонецЕсли;
	
	Сравнение = СравнитьВерсии(ТекущаяВерсия, ПоследняяВерсия);
	АдресПерехода = ?(ЗначениеЗаполнено(АдресРелиза), АдресРелиза, АдресРелизов);
	
	Если Сравнение = 0 Тогда
		
		Результат = Новый Структура;
		Результат.Вставить("Состояние", "Актуальная");
		Результат.Вставить("Заголовок", "Актуальная версия");
		Результат.Вставить("Подсказка", "Установлена последняя опубликованная версия на GitHub (" + ПоследняяВерсия + ")."
			+ Символы.ПС + "Страница релизов: " + АдресРелизов);
		Результат.Вставить("ЭтоГиперссылка", Ложь);
		Результат.Вставить("АдресПерехода", АдресРелизов);
		
	ИначеЕсли Сравнение < 0 Тогда
		
		Результат = Новый Структура;
		Результат.Вставить("Состояние", "ДоступноОбновление");
		Результат.Вставить("Заголовок", "Доступно обновление (" + ПоследняяВерсия + ")");
		Результат.Вставить("Подсказка", "Ваша версия: " + ТекущаяВерсия + ", на GitHub опубликована " + ПоследняяВерсия + "."
			+ Символы.ПС + "Нажмите, чтобы открыть страницу релиза.");
		Результат.Вставить("ЭтоГиперссылка", Истина);
		Результат.Вставить("АдресПерехода", АдресПерехода);
		
	Иначе
		
		// Локальная сборка новее последнего релиза на GitHub (например, 4.6 при релизе 4.5).
		Результат = Новый Структура;
		Результат.Вставить("Состояние", "НовееРелиза");
		Результат.Вставить("Заголовок", "на GitHub: " + ПоследняяВерсия);
		Результат.Вставить("Подсказка", "Ваша версия: " + ТекущаяВерсия + "."
			+ " Последний опубликованный релиз на GitHub: " + ПоследняяВерсия + "."
			+ Символы.ПС + "Нажмите, чтобы открыть страницу релизов.");
		Результат.Вставить("ЭтоГиперссылка", Истина);
		Результат.Вставить("АдресПерехода", АдресПерехода);
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

#КонецОбласти

#Область ОбработчикиСобытий
// Код процедур и функций
#КонецОбласти

#Область СлужебныйПрограммныйИнтерфейс
// Код процедур и функций
#КонецОбласти

#Область СлужебныеПроцедурыИФункции

Функция КоллекцияМетаданныхСуществует(ИмяКоллекции)
	
	Попытка
		Коллекция = Метаданные[ИмяКоллекции];
		Возврат Коллекция <> Неопределено;
	Исключение
		Возврат Ложь;
	КонецПопытки;
	
КонецФункции

Функция ИменаКартинокМетаданныхДляПоиска(ИмяКартинки)
	
	СоответствиеИмен = Новый Соответствие;
	СоответствиеИмен.Вставить("Конфигурация", "МетаданныеКонфигурация");
	СоответствиеИмен.Вставить("Раздел", "МетаданныеОбщие");
	СоответствиеИмен.Вставить("Подсистема", "МетаданныеПодсистемы");
	СоответствиеИмен.Вставить("ПараметрСеанса", "МетаданныеПараметрыСеанса");
	СоответствиеИмен.Вставить("ОбщийРеквизит", "МетаданныеОбщиеРеквизиты");
	СоответствиеИмен.Вставить("ПланОбмена", "МетаданныеПланыОбмена");
	СоответствиеИмен.Вставить("КритерийОтбора", "МетаданныеКритерииОтбора");
	СоответствиеИмен.Вставить("ОбщаяКоманда", "МетаданныеОбщиеКоманды");
	СоответствиеИмен.Вставить("Форма", "МетаданныеОбщиеФормы");
	СоответствиеИмен.Вставить("WebСервис", "МетаданныеWebСервисы");
	СоответствиеИмен.Вставить("WebСервисОперация", "МетаданныеWebСервисыОперации");
	СоответствиеИмен.Вставить("HTTPСервис", "МетаданныеHTTPСервисы");
	СоответствиеИмен.Вставить("HTTPШаблонURL", "МетаданныеHTTPСервисыШаблоныURL");
	СоответствиеИмен.Вставить("HTTPМетод", "МетаданныеHTTPСервисыШаблоныURLМетоды");
	СоответствиеИмен.Вставить("Константа", "МетаданныеКонстанты");
	СоответствиеИмен.Вставить("Справочник", "МетаданныеСправочники");
	СоответствиеИмен.Вставить("Документ", "МетаданныеДокументы");
	СоответствиеИмен.Вставить("ЖурналДокументов", "МетаданныеЖурналыДокументов");
	СоответствиеИмен.Вставить("МетаданныеКоманды", "МетаданныеКоманды");
	СоответствиеИмен.Вставить("Отчет", "МетаданныеОтчеты");
	СоответствиеИмен.Вставить("Обработка", "МетаданныеОбработки");
	СоответствиеИмен.Вставить("ПланВидовХарактеристик", "МетаданныеПланыВидовХарактеристик");
	СоответствиеИмен.Вставить("ПланСчетов", "МетаданныеПланыСчетов");
	СоответствиеИмен.Вставить("ПланВидовРасчета", "МетаданныеПланыВидовРасчета");
	СоответствиеИмен.Вставить("РегистрСведений", "МетаданныеРегистрыСведений");
	СоответствиеИмен.Вставить("РегистрНакопления", "МетаданныеРегистрыНакопления");
	СоответствиеИмен.Вставить("РегистрБухгалтерии", "МетаданныеРегистрыБухгалтерии");
	СоответствиеИмен.Вставить("РегистрРасчета", "МетаданныеРегистрыРасчета");
	СоответствиеИмен.Вставить("БизнесПроцесс", "МетаданныеБизнесПроцессы");
	СоответствиеИмен.Вставить("Задача", "МетаданныеЗадачи");
	СоответствиеИмен.Вставить("ВнешнийИсточникДанных", "МетаданныеВнешниеИсточникиДанных");
	
	ИменаДляПоиска = Новый Массив;
	
	ИмяИзКоллекции = СоответствиеИмен.Получить(ИмяКартинки);
	Если ИмяИзКоллекции <> Неопределено Тогда
		ИменаДляПоиска.Добавить(ИмяИзКоллекции);
	КонецЕсли;
	
	ИменаДляПоиска.Добавить(ИмяКартинки);
	
	Если ИмяКартинки = "Конфигурация" Тогда
		ИменаДляПоиска.Добавить("МетаданныеОсновнаяКонфигурация");
	ИначеЕсли ИмяКартинки = "Раздел" Тогда
		ИменаДляПоиска.Добавить("Папка");
	ИначеЕсли ИмяКартинки = "ОбщаяКоманда" Тогда
		ИменаДляПоиска.Добавить("Команда");
	КонецЕсли;
	
	Возврат ИменаДляПоиска;
	
КонецФункции

Процедура ДобавитьОписаниеКоллекцииМетаданных(Коллекции, Имя, Синоним, ГруппаОбщие, Порядок, ИмяКартинки, ИмяТипаОбъекта = "")
	
	Если НЕ КоллекцияМетаданныхСуществует(Имя) Тогда
		Возврат;
	КонецЕсли;
	
	Если ПустаяСтрока(ИмяТипаОбъекта) Тогда
		ИмяТипаОбъекта = ИмяКартинки;
	КонецЕсли;
	
	Коллекции.Добавить(Новый Структура(
		"Имя, Синоним, ГруппаОбщие, Порядок, Картинка, ИмяТипаОбъекта",
		Имя,
		Синоним,
		ГруппаОбщие,
		Порядок,
		КартинкаКоллекцииМетаданных(ИмяКартинки),
		ИмяТипаОбъекта));
	
КонецПроцедуры

Функция ПолучитьТипыОшибок() 
	
	ЗначениеКонстанты = Неопределено;
	
	Попытка
		ЗначениеКонстанты = Константы.УД_ТипОшибокДляУведомленийОбОшибках_tg.Получить();
	Исключение
		ЗначениеКонстанты = Неопределено;
	КонецПопытки;
	
	Если ЗначениеКонстанты = Неопределено Тогда
		ПометкиТаблицыУровней = Новый Структура("Первая, Вторая", Ложь, Ложь);
	Иначе
		ПометкиТаблицыУровней = Новый Структура("Первая, Вторая");
		
		РазделеннаяСтрока = СтрРазделить(ЗначениеКонстанты, ";");
		
		ПометкиТаблицыУровней.Первая = ?(РазделеннаяСтрока.Количество() > 0 И РазделеннаяСтрока[0] = "Истина", Истина, Ложь);
		ПометкиТаблицыУровней.Вторая = ?(РазделеннаяСтрока.Количество() > 1 И РазделеннаяСтрока[1] = "Истина", Истина, Ложь);
	КонецЕсли;
	
	Возврат ПометкиТаблицыУровней;
	
КонецФункции

Функция СоздатьПодложкуДляЖурнала(Колонки)
	
	масКолонок = СтрРазделить(Колонки, ",");
	
	Таблица = Новый ТаблицаЗначений;
	
	Для Каждого Колонка Из масКолонок Цикл
		
		Если Колонка = "Данные" Или Колонка = "Метаданные" Или Колонка = "ПредставлениеМетаданных" Тогда
			Продолжить;
		КонецЕслИ;
		
		Если Колонка = "Дата" Тогда
			ТипКолонки = ОбщегоНазначения.ОписаниеТипаДата(ЧастиДаты.ДатаВремя);
		Иначе
			ТипКолонки = ОбщегоНазначения.ОписаниеТипаСтрока(500);
		КонецЕсли;
		
		Таблица.Колонки.Добавить(Колонка, ТипКолонки);
	КонецЦикла;
	
	Возврат Таблица;
	
КонецФункции

Функция ПолучитьТекущегоПользователя(ПользовательСтроки) Экспорт
	
	ИмяПользователяСтрокой = ПользователиИнформационнойБазы.НайтиПоУникальномуИдентификатору(Новый УникальныйИдентификатор(ПользовательСтроки)).ПолноеИмя;
	Возврат Справочники.Пользователи.НайтиПоНаименованию(ИмяПользователяСтрокой);
	
КонецФункции

Функция ОбъектУжеИзвестен(СтруктураМетаданных, КлючПользователя)
	
	ОбъектУжеЕсть = Ложь;
	
	Для Каждого Мета Из КлючПользователя Цикл		
		Если Мета.ТипМетаданного = СтруктураМетаданных.ТипМетаданного И Мета.ИмяМетаданного = СтруктураМетаданных.ИмяМетаданного Тогда			
			ОбъектУжеЕсть = Истина;
			Прервать;			
		КонецЕсли;		
	КонецЦикла;		
	
	Возврат ОбъектУжеЕсть;
	
КонецФункции

Функция ПользовательВключенВГруппуДоступа(ГруппаДоступа, Пользователь)
	
	УстановитьПривилегированныйРежим(Истина);
	
	Запрос = Новый Запрос;
	Запрос.УстановитьПараметр("ГруппаДоступа", ГруппаДоступа);
	Запрос.УстановитьПараметр("Пользователь", Пользователь);
	Запрос.Текст =
	"ВЫБРАТЬ
	|	ИСТИНА КАК ЗначениеИстина
	|ИЗ
	|	Справочник.ГруппыДоступа.Пользователи КАК ГруппыДоступаПользователи
	|ГДЕ
	|	ГруппыДоступаПользователи.Ссылка = &ГруппаДоступа
	|	И ГруппыДоступаПользователи.Пользователь = &Пользователь";
	
	Возврат НЕ Запрос.Выполнить().Пустой();
	
КонецФункции

Функция РазрешеноИзменениеСоставаПользователейГруппы(ГруппаДоступа)
	
	Если ПользовательИБПолноправный Тогда
		Возврат Истина;
	КонецЕсли;
	
	УстановитьПривилегированныйРежим(Истина);
	
	Запрос = Новый Запрос;
	Запрос.УстановитьПараметр("ГруппаДоступа",              ГруппаДоступа);
	Запрос.УстановитьПараметр("АвторизованныйПользователь", Пользователи.АвторизованныйПользователь());
	Запрос.Текст =
	"ВЫБРАТЬ ПЕРВЫЕ 1
	|	ИСТИНА КАК ЗначениеИстина
	|ИЗ
	|	Справочник.ГруппыДоступа КАК ГруппыДоступа
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ РегистрСведений.СоставыГруппПользователей КАК СоставыГруппПользователей
	|		ПО (СоставыГруппПользователей.Пользователь = &АвторизованныйПользователь)
	|			И (СоставыГруппПользователей.ГруппаПользователей = ГруппыДоступа.Ответственный)
	|			И (ГруппыДоступа.Ссылка = &ГруппаДоступа)";
	
	Возврат НЕ Запрос.Выполнить().Пустой();
	
КонецФункции

Функция РезультатПриСбоеПроверки(ТекущаяВерсия, АдресРелизов, ТекстОшибки)
	
	Результат = Новый Структура;
	Результат.Вставить("Состояние", "Недоступна");
	Результат.Вставить("Заголовок", "Нет связи");
	Результат.Вставить("Подсказка", "Не удалось проверить наличие обновлений (нет доступа в интернет)."
		+ Символы.ПС + ТекстОшибки
		+ Символы.ПС + "Текущая версия обработки: " + ТекущаяВерсия + "."
		+ Символы.ПС + "Страница релизов: " + АдресРелизов);
	Результат.Вставить("ЭтоГиперссылка", Истина);
	Результат.Вставить("АдресПерехода", АдресРелизов);
	
	Возврат Результат;
	
КонецФункции

Функция ПолучитьПоследнийРелизСGitHub()
	
	Сервер = "api.github.com";
	Ресурс = "/repos/VorozhbitDM/Access-control-1C/releases/latest";
	ТаймаутСекунд = 10;
	
	Соединение = Новый HTTPСоединение(Сервер,,,,, ТаймаутСекунд, Новый ЗащищенноеСоединениеOpenSSL);
	Запрос = Новый HTTPЗапрос(Ресурс);
	Запрос.Заголовки.Вставить("User-Agent", "AccessControl-1C/" + ВерсияОбработки());
	Запрос.Заголовки.Вставить("Accept", "application/vnd.github+json");
	
	Ответ = Соединение.Получить(Запрос);
	
	Если Ответ.КодСостояния <> 200 Тогда
		ВызватьИсключение "HTTP " + Формат(Ответ.КодСостояния, "ЧГ=0") + " при запросе последнего релиза.";
	КонецЕсли;
	
	Тело = Ответ.ПолучитьТелоКакСтроку();
	
	Если ПустаяСтрока(Тело) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Чтение = Новый ЧтениеJSON;
	Чтение.УстановитьСтроку(Тело);
	Данные = ПрочитатьJSON(Чтение, Истина);
	
	ТегВерсии = ЗначениеПоляJSON(Данные, "tag_name");
	АдресРелиза = ЗначениеПоляJSON(Данные, "html_url");
	
	Если НЕ ЗначениеЗаполнено(ТегВерсии) Тогда
		ВызватьИсключение "В ответе GitHub (HTTP 200) нет поля tag_name.";
	КонецЕсли;
	
	Результат = Новый Структура;
	Результат.Вставить("ТегВерсии", ТегВерсии);
	Результат.Вставить("АдресРелиза", ?(ЗначениеЗаполнено(АдресРелиза), АдресРелиза, URLСтраницыРелизов()));
	
	Возврат Результат;
	
КонецФункции

Функция ЗначениеПоляJSON(ДанныеJSON, ИмяПоля)
	
	Если ДанныеJSON = Неопределено Тогда
		Возврат "";
	КонецЕсли;
	
	Если ТипЗнч(ДанныеJSON) = Тип("Структура") И ДанныеJSON.Свойство(ИмяПоля) Тогда
		Возврат ДанныеJSON[ИмяПоля];
	КонецЕсли;
	
	Если ТипЗнч(ДанныеJSON) = Тип("Соответствие") Тогда
		Значение = ДанныеJSON.Получить(ИмяПоля);
		Если Значение <> Неопределено Тогда
			Возврат Значение;
		КонецЕсли;
	КонецЕсли;
	
	Попытка
		Возврат ДанныеJSON[ИмяПоля];
	Исключение
		Возврат "";
	КонецПопытки;
	
КонецФункции

Функция НормализоватьСтрокуВерсии(СтрокаВерсии)
	
	Результат = СокрЛП(СтрокаВерсии);
	
	Если ПустаяСтрока(Результат) Тогда
		Возврат "";
	КонецЕсли;
	
	Если Лев(НРег(Результат), 1) = "v" Тогда
		Результат = СокрЛП(Сред(Результат, 2));
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция СравнитьВерсии(Версия1, Версия2)
	
	Части1 = РазобратьВерсиюНаЧисла(Версия1);
	Части2 = РазобратьВерсиюНаЧисла(Версия2);
	МаксИндекс = Макс(Части1.ВГраница(), Части2.ВГраница());
	
	Для Индекс = 0 По МаксИндекс Цикл
		
		Число1 = ?(Индекс <= Части1.ВГраница(), Части1[Индекс], 0);
		Число2 = ?(Индекс <= Части2.ВГраница(), Части2[Индекс], 0);
		
		Если Число1 < Число2 Тогда
			Возврат -1;
		ИначеЕсли Число1 > Число2 Тогда
			Возврат 1;
		КонецЕсли;
		
	КонецЦикла;
	
	Возврат 0;
	
КонецФункции

Функция РазобратьВерсиюНаЧисла(СтрокаВерсии)
	
	Нормализованная = НормализоватьСтрокуВерсии(СтрокаВерсии);
	ЧастиСтроки = СтрРазделить(Нормализованная, ".", Ложь);
	Результат = Новый Массив;
	
	Для Каждого Часть Из ЧастиСтроки Цикл
		
		ЧисловаяЧасть = "";
		
		Для Позиция = 1 По СтрДлина(Часть) Цикл
			
			Символ = Сред(Часть, Позиция, 1);
			
			Если Символ >= "0" И Символ <= "9" Тогда
				ЧисловаяЧасть = ЧисловаяЧасть + Символ;
			Иначе
				Прервать;
			КонецЕсли;
			
		КонецЦикла;
		
		Результат.Добавить(?(ЗначениеЗаполнено(ЧисловаяЧасть), Число(ЧисловаяЧасть), 0));
		
	КонецЦикла;
	
	Если Результат.Количество() = 0 Тогда
		Результат.Добавить(0);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

#КонецОбласти

#Область ДлительныеОперацииОтчетов

Функция ПолучитьНастройкиДлительныхОперацийОтчетов() Экспорт
	
	Настройки = Новый Структура;
	Настройки.Вставить("ИспользоватьФон", Ложь);
	Настройки.Вставить("ВерсияБСП", "");
	Настройки.Вставить("ПричинаОтказа", "");
	
	Если Метаданные.ОбщиеМодули.Найти("СтандартныеПодсистемыСервер") = Неопределено
		ИЛИ Метаданные.ОбщиеМодули.Найти("ДлительныеОперации") = Неопределено Тогда
		Настройки.ПричинаОтказа = "Подсистема длительных операций БСП недоступна.";
		Возврат Настройки;
	КонецЕсли;
	
	ВерсияБСП = СтандартныеПодсистемыСервер.ВерсияБиблиотеки();
	Настройки.ВерсияБСП = ВерсияБСП;
	
	Если СравнитьВерсии(ВерсияБСП, "3.1.5") < 0 Тогда
		Настройки.ПричинаОтказа = "Требуется БСП 3.1.5 или новее (текущая: " + Лев(ВерсияБСП, 10) + ").";
		Возврат Настройки;
	КонецЕсли;
	
	Настройки.ИспользоватьФон = Истина;
	
	Возврат Настройки;
	
КонецФункции

// Двоичные данные .epf обязательны для ВнешнийОтчетОбработка в ДлительныеОперации.
//
// Параметры:
//  АдресИзВременногоХранилища - Строка - адрес, помещённый на клиенте;
//  СсылкаДопОбработки - СправочникСсылка.ДополнительныеОтчетыИОбработки - при запуске из БСП.
//
Функция ПолучитьДвоичныеДанныеОбработкиДляФона(ОбработкаОбъект, АдресИзВременногоХранилища = "",
	СсылкаДопОбработки = Неопределено) Экспорт
	
	Если ЗначениеЗаполнено(АдресИзВременногоХранилища) Тогда
		ДанныеИзХранилища = ПолучитьИзВременногоХранилища(АдресИзВременногоХранилища);
		Если ТипЗнч(ДанныеИзХранилища) = Тип("ДвоичныеДанные") Тогда
			Возврат ДанныеИзХранилища;
		КонецЕсли;
	КонецЕсли;
	
	ДвоичныеДанные = ДвоичныеДанныеОбработкиИзСправочникаПоСсылке(СсылкаДопОбработки);
	
	Если ДвоичныеДанные <> Неопределено Тогда
		Возврат ДвоичныеДанные;
	КонецЕсли;
	
	ДвоичныеДанные = ДвоичныеДанныеПоИспользуемомуИмениФайлаОбъекта(ЭтотОбъект);
	
	Если ДвоичныеДанные <> Неопределено Тогда
		Возврат ДвоичныеДанные;
	КонецЕсли;
	
	Если ОбработкаОбъект <> ЭтотОбъект Тогда
		ДвоичныеДанные = ДвоичныеДанныеПоИспользуемомуИмениФайлаОбъекта(ОбработкаОбъект);
		Если ДвоичныеДанные <> Неопределено Тогда
			Возврат ДвоичныеДанные;
		КонецЕсли;
	КонецЕсли;
	
	ИмяОбъекта = ОбработкаОбъект.Метаданные().Имя;
	ДвоичныеДанные = ДвоичныеДанныеОбработкиИзСправочника(ИмяОбъекта);
	
	Если ДвоичныеДанные <> Неопределено Тогда
		Возврат ДвоичныеДанные;
	КонецЕсли;
	
	ДвоичныеДанные = ДвоичныеДанныеОбработкиЧерезЗапись(ЭтотОбъект);
	
	Если ДвоичныеДанные <> Неопределено Тогда
		Возврат ДвоичныеДанные;
	КонецЕсли;
	
	Возврат ДвоичныеДанныеОбработкиЧерезЗапись(ОбработкаОбъект);
	
КонецФункции

Функция ДвоичныеДанныеПоИспользуемомуИмениФайлаОбъекта(ОбработкаОбъект)
	
	Попытка
		Возврат ДвоичныеДанныеПоИспользуемомуИмениФайла(ОбработкаОбъект.ИспользуемоеИмяФайла());
	Исключение
		Возврат Неопределено;
	КонецПопытки;
	
КонецФункции

// ИспользуемоеИмяФайла() — путь к .epf или адрес e1cib/tempstorage с двоичными данными.
Функция ДвоичныеДанныеПоИспользуемомуИмениФайла(ИспользуемоеИмя) Экспорт
	
	Если НЕ ЗначениеЗаполнено(ИспользуемоеИмя) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Если СтрНачинаетсяС(НРег(ИспользуемоеИмя), "e1cib") Тогда
		ДанныеИзХранилища = ПолучитьИзВременногоХранилища(ИспользуемоеИмя);
		Если ТипЗнч(ДанныеИзХранилища) = Тип("ДвоичныеДанные") Тогда
			Возврат ДанныеИзХранилища;
		КонецЕсли;
		Возврат Неопределено;
	КонецЕсли;
	
	ФайлОбработки = Новый Файл(ИспользуемоеИмя);
	
	Если ФайлОбработки.Существует() Тогда
		Возврат Новый ДвоичныеДанные(ИспользуемоеИмя);
	КонецЕсли;
	
	Возврат Неопределено;
	
КонецФункции

Функция ДвоичныеДанныеОбработкиИзСправочникаПоСсылке(СсылкаДопОбработки)
	
	Если НЕ ЗначениеЗаполнено(СсылкаДопОбработки) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Попытка
		
		Если Метаданные.ОбщиеМодули.Найти("ОбщегоНазначения") <> Неопределено Тогда
			ХранилищеОбработки = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(СсылкаДопОбработки, "ХранилищеОбработки");
			Если ХранилищеОбработки <> Неопределено Тогда
				Возврат ХранилищеОбработки.Получить();
			КонецЕсли;
		КонецЕсли;
		
		ХранилищеОбработки = СсылкаДопОбработки.ХранилищеОбработки;
		Если ХранилищеОбработки <> Неопределено Тогда
			Возврат ХранилищеОбработки.Получить();
		КонецЕсли;
		
	Исключение
	КонецПопытки;
	
	Возврат Неопределено;
	
КонецФункции

Функция ДвоичныеДанныеОбработкиИзСправочника(ИмяОбъектаОбработки)
	
	Если Метаданные.Справочники.Найти("ДополнительныеОтчетыИОбработки") = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Попытка
		
		Запрос = Новый Запрос;
		Запрос.Текст =
		"ВЫБРАТЬ ПЕРВЫЕ 1
		|	ДополнительныеОтчетыИОбработки.ХранилищеОбработки КАК ХранилищеОбработки
		|ИЗ
		|	Справочник.ДополнительныеОтчетыИОбработки КАК ДополнительныеОтчетыИОбработки
		|ГДЕ
		|	НЕ ДополнительныеОтчетыИОбработки.ПометкаУдаления
		|	И (ДополнительныеОтчетыИОбработки.ИмяОбъекта = &ИмяОбъекта
		|		ИЛИ ДополнительныеОтчетыИОбработки.ИмяОбъекта ПОДОБНО &ИмяОбъектаПодобно)";
		Запрос.УстановитьПараметр("ИмяОбъекта", ИмяОбъектаОбработки);
		Запрос.УстановитьПараметр("ИмяОбъектаПодобно", "%" + ИмяОбъектаОбработки + "%");
		
		УстановитьПривилегированныйРежим(Истина);
		Выборка = Запрос.Выполнить().Выбрать();
		УстановитьПривилегированныйРежим(Ложь);
		
		Если Выборка.Следующий() И ЗначениеЗаполнено(Выборка.ХранилищеОбработки) Тогда
			Возврат Выборка.ХранилищеОбработки.Получить();
		КонецЕсли;
		
	Исключение
	КонецПопытки;
	
	Возврат Неопределено;
	
КонецФункции

Функция ДвоичныеДанныеОбработкиЧерезЗапись(ОбработкаОбъект)
	
	Попытка
		ИмяВременногоФайла = КаталогВременныхФайлов()
			+ ПолучитьРазделительПути()
			+ "AC_"
			+ СтрЗаменить(Строка(Новый УникальныйИдентификатор), "-", "")
			+ ".epf";
		ОбработкаОбъект.Записать(ИмяВременногоФайла);
		ДвоичныеДанные = Новый ДвоичныеДанные(ИмяВременногоФайла);
		УдалитьФайлы(ИмяВременногоФайла);
		Возврат ДвоичныеДанные;
	Исключение
		Возврат Неопределено;
	КонецПопытки;
	
КонецФункции

// Запуск фонового задания для внешней обработки (как в типовых обработках БСП).
//
// Параметры:
//  ИмяМетода - Строка - экспортная процедура модуля объекта без префикса;
//  ПолноеИмяОбработки - Строка - Метаданные().ПолноеИмя() текущей обработки;
//  ДвоичныеДанныеОбработки - ДвоичныеДанные, Неопределено - файл .epf для фонового задания.
//
Функция ЗапуститьДлительнуюОперациюОтчетов(ИмяМетода, ПараметрыПроцедуры, ИдентификаторФормы, НаименованиеЗадания,
	ПолноеИмяОбработки, ДвоичныеДанныеОбработки = Неопределено) Экспорт
	
	Настройки = ПолучитьНастройкиДлительныхОперацийОтчетов();
	РезультатЗапуска = Новый Структура("ИспользоватьФон, Операция, ПричинаОтказа, Настройки",
		Ложь, Неопределено, Настройки.ПричинаОтказа, Настройки);
	
	Если НЕ Настройки.ИспользоватьФон Тогда
		Возврат РезультатЗапуска;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(ПолноеИмяОбработки) Тогда
		РезультатЗапуска.ПричинаОтказа = "Не удалось определить полное имя обработки.";
		Возврат РезультатЗапуска;
	КонецЕсли;
	
	ИмяПроцедуры = ПолноеИмяОбработки + ".МодульОбъекта." + ИмяМетода;
	
	Если ДвоичныеДанныеОбработки = Неопределено Тогда
		РезультатЗапуска.ПричинаОтказа = "Не удалось получить двоичные данные внешней обработки для фонового задания.";
		Возврат РезультатЗапуска;
	КонецЕсли;
	
	ПараметрыВыполнения = ДлительныеОперации.ПараметрыВыполненияВФоне(ИдентификаторФормы);
	ПараметрыВыполнения.НаименованиеФоновогоЗадания = НаименованиеЗадания;
	ПараметрыВыполнения.ВнешнийОтчетОбработка = ДвоичныеДанныеОбработки;
	УстановитьСвойствоПараметровДлительнойОперации(ПараметрыВыполнения, "ПрерватьВыполнениеЕслиЗакрытаФорма", Истина);
	
	Операция = ДлительныеОперации.ВыполнитьВФоне(ИмяПроцедуры, ПараметрыПроцедуры, ПараметрыВыполнения);
	
	РезультатЗапуска.ИспользоватьФон = Истина;
	РезультатЗапуска.Операция = Операция;
	РезультатЗапуска.ПричинаОтказа = "";
	
	Возврат РезультатЗапуска;
	
КонецФункции

Процедура УстановитьСвойствоПараметровДлительнойОперации(ПараметрыВыполнения, ИмяСвойства, Значение)
	
	Попытка
		ПараметрыВыполнения[ИмяСвойства] = Значение;
	Исключение
	КонецПопытки;
	
КонецПроцедуры

#КонецОбласти

#Область ОтчетыПоРолям

Процедура СформироватьДанныеОтчетовПоРолям(Параметры, АдресРезультата) Экспорт
	
	ДлительныеОперации.СообщитьПрогресс(0, "Подготовка отчётов по ролям…");
	
	ТаблицаРолейБезПользователей = НоваяТаблицаРолейОтчета();
	ТаблицаРолейВнеПрофилей = ТаблицаРолейБезПользователей.Скопировать();
	
	ВсегоРолейКонфигурации = Метаданные.Роли.Количество();
	
	ДлительныеОперации.СообщитьПрогресс(10, "Загрузка данных о назначениях…");
	
	ИспользуемыеСПользователями = ПолучитьИменаИспользуемыхРолейОтчета("БезПользователей");
	ИспользуемыеВПрофилях = ПолучитьИменаИспользуемыхРолейОтчета("ВнеПрофилей");
	ПрофилиПоРолям = ПолучитьКоличествоПрофилейПоРолямОтчета();
	
	ДлительныеОперации.СообщитьПрогресс(25, "Анализ ролей конфигурации…");
	
	Индекс = 0;
	
	Для Каждого РольМета Из Метаданные.Роли Цикл
		
		Индекс = Индекс + 1;
		
		Если Индекс % 25 = 0 И ВсегоРолейКонфигурации > 0 Тогда
			Процент = 25 + Окр(55 * Индекс / ВсегоРолейКонфигурации);
			ДлительныеОперации.СообщитьПрогресс(Процент,
				СтрШаблон("Анализ ролей: %1 из %2…", Индекс, ВсегоРолейКонфигурации));
		КонецЕсли;
		
		Если ИспользуемыеСПользователями[РольМета.Имя] = Неопределено Тогда
			НоваяСтрока = ТаблицаРолейБезПользователей.Добавить();
			ЗаполнитьСтрокуРолиОтчета(НоваяСтрока, РольМета, ПрофилиПоРолям);
		КонецЕсли;
		
		Если ИспользуемыеВПрофилях[РольМета.Имя] = Неопределено Тогда
			НоваяСтрока = ТаблицаРолейВнеПрофилей.Добавить();
			ЗаполнитьСтрокуРолиОтчета(НоваяСтрока, РольМета, ПрофилиПоРолям);
		КонецЕсли;
		
	КонецЦикла;
	
	ТаблицаРолейБезПользователей.Сортировать("Синоним");
	ТаблицаРолейВнеПрофилей.Сортировать("Синоним");
	
	ДлительныеОперации.СообщитьПрогресс(85, "Загрузка прав из регистра…");
	
	ОбъектыПоРолям = ПолучитьКоличествоПравПоРолямОтчета();
	ЗаполнитьКоличествоОбъектовОтчета(ТаблицаРолейБезПользователей, ОбъектыПоРолям);
	ЗаполнитьКоличествоОбъектовОтчета(ТаблицаРолейВнеПрофилей, ОбъектыПоРолям);
	
	ДлительныеОперации.СообщитьПрогресс(95, "Завершение…");
	
	Результат = Новый Структура;
	Результат.Вставить("ВсегоРолейКонфигурации", ВсегоРолейКонфигурации);
	Результат.Вставить("ТаблицаРолейБезПользователей", ТаблицаРолейБезПользователей);
	Результат.Вставить("ТаблицаРолейВнеПрофилей", ТаблицаРолейВнеПрофилей);
	Результат.Вставить("КоличествоБезПользователей", ТаблицаРолейБезПользователей.Количество());
	Результат.Вставить("КоличествоВнеПрофилей", ТаблицаРолейВнеПрофилей.Количество());
	
	ПоместитьВоВременноеХранилище(Результат, АдресРезультата);
	ДлительныеОперации.СообщитьПрогресс(100, "Готово");
	
КонецПроцедуры

Процедура ПодсчитатьОбъектыМетаданныхОтчетовПоРолям(Параметры, АдресРезультата) Экспорт
	
	ТаблицаРолейБезПользователей = ТаблицаРолейОтчетаИзВременногоХранилища(Параметры.АдресТаблицыБезПользователей);
	ТаблицаРолейВнеПрофилей = ТаблицаРолейОтчетаИзВременногоХранилища(Параметры.АдресТаблицыВнеПрофилей);
	
	ДлительныеОперации.СообщитьПрогресс(0, "Загрузка прав из регистра…");
	
	ОбъектыПоРолям = ПолучитьКоличествоПравПоРолямОтчета();
	ВсегоРолей = КоличествоСтрокТаблицыРолей(ТаблицаРолейБезПользователей)
		+ КоличествоСтрокТаблицыРолей(ТаблицаРолейВнеПрофилей);
	Обработано = 0;
	
	ДлительныеОперации.СообщитьПрогресс(5, "Дополнение через ПравоДоступа…");
	
	Обработано = ДополнитьОбъектыПоРолямПравоДоступаОтчета(ОбъектыПоРолям, ТаблицаРолейБезПользователей, Обработано, ВсегоРолей);
	ДополнитьОбъектыПоРолямПравоДоступаОтчета(ОбъектыПоРолям, ТаблицаРолейВнеПрофилей, Обработано, ВсегоРолей);
	
	ЗаполнитьКоличествоОбъектовОтчета(ТаблицаРолейБезПользователей, ОбъектыПоРолям);
	ЗаполнитьКоличествоОбъектовОтчета(ТаблицаРолейВнеПрофилей, ОбъектыПоРолям);
	
	ДлительныеОперации.СообщитьПрогресс(100, "Готово");
	
	Результат = Новый Структура;
	Результат.Вставить("ТаблицаРолейБезПользователей", ТаблицаРолейБезПользователей);
	Результат.Вставить("ТаблицаРолейВнеПрофилей", ТаблицаРолейВнеПрофилей);
	
	ПоместитьВоВременноеХранилище(Результат, АдресРезультата);
	
КонецПроцедуры

Функция НоваяТаблицаРолейОтчета()
	
	ТаблицаРолей = Новый ТаблицаЗначений;
	ТаблицаРолей.Колонки.Добавить("Роль");
	ТаблицаРолей.Колонки.Добавить("Синоним");
	ТаблицаРолей.Колонки.Добавить("Комментарий");
	ТаблицаРолей.Колонки.Добавить("КоличествоПрофилей");
	ТаблицаРолей.Колонки.Добавить("КоличествоПрав");
	
	Возврат ТаблицаРолей;
	
КонецФункции

Функция ТаблицаРолейОтчетаИзВременногоХранилища(Адрес)
	
	Данные = ПолучитьИзВременногоХранилища(Адрес);
	УдалитьИзВременногоХранилища(Адрес);
	
	Возврат ПривестиКТаблицеРолейОтчета(Данные);
	
КонецФункции

Функция ПривестиКТаблицеРолейОтчета(Данные)
	
	Если Данные = Неопределено Тогда
		Возврат НоваяТаблицаРолейОтчета();
	КонецЕсли;
	
	Если ТипЗнч(Данные) = Тип("ТаблицаЗначений") Тогда
		Возврат Данные;
	КонецЕсли;
	
	Результат = НоваяТаблицаРолейОтчета();
	
	Для Каждого СтрокаДанных Из Данные Цикл
		ЗаполнитьЗначенияСвойств(Результат.Добавить(), СтрокаДанных);
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция КоличествоСтрокТаблицыРолей(Таблица)
	
	Если Таблица = Неопределено Тогда
		Возврат 0;
	КонецЕсли;
	
	Если ТипЗнч(Таблица) = Тип("ТаблицаЗначений") Тогда
		Возврат Таблица.Количество();
	КонецЕсли;
	
	Количество = 0;
	
	Для Каждого СтрокаДанных Из Таблица Цикл
		Количество = Количество + 1;
	КонецЦикла;
	
	Возврат Количество;
	
КонецФункции

Процедура ЗаполнитьСтрокуРолиОтчета(СтрокаРоли, РольМета, ПрофилиПоРолям)
	
	СтрокаРоли.Роль = РольМета.Имя;
	СтрокаРоли.Синоним = РольМета.Синоним;
	СтрокаРоли.Комментарий = РольМета.Комментарий;
	
	КоличествоПрофилей = ПрофилиПоРолям[РольМета.Имя];
	СтрокаРоли.КоличествоПрофилей = ?(КоличествоПрофилей = Неопределено, 0, КоличествоПрофилей);
	СтрокаРоли.КоличествоПрав = 0;
	
КонецПроцедуры

Процедура ЗаполнитьКоличествоОбъектовОтчета(ТаблицаРолей, ОбъектыПоРолям)
	
	Для Каждого СтрокаРоли Из ТаблицаРолей Цикл
		КоличествоОбъектов = ОбъектыПоРолям[СтрокаРоли.Роль];
		СтрокаРоли.КоличествоПрав = ?(КоличествоОбъектов = Неопределено, 0, КоличествоОбъектов);
	КонецЦикла;
	
КонецПроцедуры

Функция ДополнитьОбъектыПоРолямПравоДоступаОтчета(ОбъектыПоРолям, ТаблицаРолей, Обработано, ВсегоРолей)
	
	Для Каждого СтрокаРоли Из ТаблицаРолей Цикл
		
		Обработано = Обработано + 1;
		
		Если ВсегоРолей > 0 И Обработано % 3 = 0 Тогда
			Процент = 5 + Окр(90 * Обработано / ВсегоРолей);
			ДлительныеОперации.СообщитьПрогресс(Процент,
				СтрШаблон("Подсчёт объектов метаданных: %1 из %2…", Обработано, ВсегоРолей));
		КонецЕсли;
		
		КоличествоИзРегистра = ОбъектыПоРолям[СтрокаРоли.Роль];
		
		Если КоличествоИзРегистра <> Неопределено И КоличествоИзРегистра > 0 Тогда
			Продолжить;
		КонецЕсли;
		
		РольМета = Метаданные.Роли.Найти(СтрокаРоли.Роль);
		
		Если РольМета = Неопределено Тогда
			Продолжить;
		КонецЕсли;
		
		ОбъектыПоРолям.Вставить(СтрокаРоли.Роль, ПодсчитатьОбъектыРолиПравоДоступаОтчета(РольМета));
		
	КонецЦикла;
	
	Возврат Обработано;
	
КонецФункции

Функция ПолучитьКоличествоПравПоРолямОтчета()
	
	Результат = Новый Соответствие;
	
	Если Метаданные.РегистрыСведений.Найти("ПраваРолей") = Неопределено Тогда
		Возврат Результат;
	КонецЕсли;
	
	Запрос = Новый Запрос;
	Запрос.Текст =
	"ВЫБРАТЬ
	|	ПраваРолей.Роль.Имя КАК Роль,
	|	КОЛИЧЕСТВО(РАЗЛИЧНЫЕ ПраваРолей.ОбъектМетаданных) КАК КоличествоПрав
	|ИЗ
	|	РегистрСведений.ПраваРолей КАК ПраваРолей
	|
	|СГРУППИРОВАТЬ ПО
	|	ПраваРолей.Роль.Имя";
	
	УстановитьПривилегированныйРежим(Истина);
	Выборка = Запрос.Выполнить().Выбрать();
	УстановитьПривилегированныйРежим(Ложь);
	
	Пока Выборка.Следующий() Цикл
		Результат.Вставить(Выборка.Роль, Выборка.КоличествоПрав);
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция ПодсчитатьОбъектыРолиПравоДоступаОтчета(РольМета)
	
	СписокОбъектов = ПолучитьСписокОбъектовРолиПравоДоступаОтчета(РольМета);
	
	Если СписокОбъектов = Неопределено Тогда
		Возврат 0;
	КонецЕсли;
	
	Возврат СписокОбъектов.Количество();
	
КонецФункции

Функция ПолучитьСписокОбъектовРолиПравоДоступаОтчета(РольМета)
	
	СписокОбъектов = Новый Массив;
	УжеДобавлено = Новый Соответствие;
	
	Для Каждого ИмяКоллекции Из ИменаКоллекцийОбъектовМетаданныхОтчета() Цикл
		
		Попытка
			КоллекцияМетаданных = Метаданные[ИмяКоллекции];
		Исключение
			Продолжить;
		КонецПопытки;
		
		Для Каждого ОбъектМетаданных Из КоллекцияМетаданных Цикл
			
			Если НЕ ОбъектМетаданныхСодержитПраваРолиОтчета(ОбъектМетаданных, РольМета) Тогда
				Продолжить;
			КонецЕсли;
			
			Попытка
				ПредставлениеОбъекта = ОбъектМетаданных.ПолноеИмя();
			Исключение
				Продолжить;
			КонецПопытки;
			
			Если УжеДобавлено[ПредставлениеОбъекта] <> Неопределено Тогда
				Продолжить;
			КонецЕсли;
			
			УжеДобавлено.Вставить(ПредставлениеОбъекта, Истина);
			СписокОбъектов.Добавить(ПредставлениеОбъекта);
			
		КонецЦикла;
		
	КонецЦикла;
	
	СписокДляСортировки = Новый СписокЗначений;
	
	Для Каждого ПредставлениеОбъекта Из СписокОбъектов Цикл
		СписокДляСортировки.Добавить(ПредставлениеОбъекта);
	КонецЦикла;
	
	СписокДляСортировки.СортироватьПоЗначению();
	
	Результат = Новый Массив;
	
	Для Каждого ЭлементСписка Из СписокДляСортировки Цикл
		Результат.Добавить(ЭлементСписка.Значение);
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция ОбъектМетаданныхСодержитПраваРолиОтчета(ОбъектМетаданных, РольМета)
	
	Попытка
		Если ПравоДоступа("Использование", ОбъектМетаданных, РольМета) Тогда
			Возврат Истина;
		КонецЕсли;
	Исключение
	КонецПопытки;
	
	Попытка
		Если ПравоДоступа("Чтение", ОбъектМетаданных, РольМета) Тогда
			Возврат Истина;
		КонецЕсли;
	Исключение
	КонецПопытки;
	
	Попытка
		Если ПравоДоступа("Просмотр", ОбъектМетаданных, РольМета) Тогда
			Возврат Истина;
		КонецЕсли;
	Исключение
	КонецПопытки;
	
	Возврат Ложь;
	
КонецФункции

Функция ИменаКоллекцийОбъектовМетаданныхОтчета()
	
	Коллекции = Новый Массив;
	Коллекции.Добавить("Подсистемы");
	Коллекции.Добавить("ОбщиеФормы");
	Коллекции.Добавить("Константы");
	Коллекции.Добавить("Справочники");
	Коллекции.Добавить("ЖурналыДокументов");
	Коллекции.Добавить("Документы");
	Коллекции.Добавить("Отчеты");
	Коллекции.Добавить("Обработки");
	Коллекции.Добавить("ПланыВидовХарактеристик");
	Коллекции.Добавить("ПланыВидовРасчета");
	Коллекции.Добавить("РегистрыСведений");
	Коллекции.Добавить("РегистрыНакопления");
	Коллекции.Добавить("РегистрыБухгалтерии");
	Коллекции.Добавить("РегистрыРасчета");
	Коллекции.Добавить("БизнесПроцессы");
	Коллекции.Добавить("Задачи");
	Коллекции.Добавить("ОбщиеКоманды");
	
	Возврат Коллекции;
	
КонецФункции

Функция ПолучитьКоличествоПрофилейПоРолямОтчета()
	
	Запрос = Новый Запрос;
	Запрос.Текст =
	"ВЫБРАТЬ
	|	ПрофилиГруппДоступаРоли.Роль.Имя КАК Роль,
	|	КОЛИЧЕСТВО(РАЗЛИЧНЫЕ ПрофилиГруппДоступаРоли.Ссылка) КАК КоличествоПрофилей
	|ИЗ
	|	Справочник.ПрофилиГруппДоступа.Роли КАК ПрофилиГруппДоступаРоли
	|ГДЕ
	|	НЕ ПрофилиГруппДоступаРоли.Ссылка.ПометкаУдаления
	|
	|СГРУППИРОВАТЬ ПО
	|	ПрофилиГруппДоступаРоли.Роль.Имя";
	
	УстановитьПривилегированныйРежим(Истина);
	Выборка = Запрос.Выполнить().Выбрать();
	УстановитьПривилегированныйРежим(Ложь);
	
	Результат = Новый Соответствие;
	
	Пока Выборка.Следующий() Цикл
		Результат.Вставить(Выборка.Роль, Выборка.КоличествоПрофилей);
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция ПолучитьИменаИспользуемыхРолейОтчета(РежимАнализа)
	
	Запрос = Новый Запрос;
	
	Если РежимАнализа = "ВнеПрофилей" Тогда
		
		Запрос.Текст =
		"ВЫБРАТЬ РАЗЛИЧНЫЕ
		|	ПрофилиГруппДоступаРоли.Роль.Имя КАК Роль
		|ИЗ
		|	Справочник.ПрофилиГруппДоступа.Роли КАК ПрофилиГруппДоступаРоли
		|ГДЕ
		|	НЕ ПрофилиГруппДоступаРоли.Ссылка.ПометкаУдаления";
		
	Иначе
		
		Запрос.Текст =
		"ВЫБРАТЬ РАЗЛИЧНЫЕ
		|	ПрофилиГруппДоступаРоли.Роль.Имя КАК Роль
		|ИЗ
		|	Справочник.ПрофилиГруппДоступа.Роли КАК ПрофилиГруппДоступаРоли
		|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.ГруппыДоступа.Пользователи КАК ГруппыДоступаПользователи
		|		ПО ПрофилиГруппДоступаРоли.Ссылка = ГруппыДоступаПользователи.Ссылка.Профиль
		|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.ГруппыПользователей.Состав КАК ГруппыПользователейСостав
		|		ПО ГруппыДоступаПользователи.Пользователь = ГруппыПользователейСостав.Ссылка
		|ГДЕ
		|	НЕ ПрофилиГруппДоступаРоли.Ссылка.ПометкаУдаления
		|	И НЕ ГруппыДоступаПользователи.Ссылка.ПометкаУдаления
		|	И (ГруппыДоступаПользователи.Пользователь ССЫЛКА Справочник.Пользователи
		|		ИЛИ ГруппыПользователейСостав.Пользователь ССЫЛКА Справочник.Пользователи)";
		
	КонецЕсли;
	
	УстановитьПривилегированныйРежим(Истина);
	Выборка = Запрос.Выполнить().Выбрать();
	УстановитьПривилегированныйРежим(Ложь);
	
	ИспользуемыеРоли = Новый Соответствие;
	
	Пока Выборка.Следующий() Цикл
		ИспользуемыеРоли.Вставить(Выборка.Роль, Истина);
	КонецЦикла;
	
	Возврат ИспользуемыеРоли;
	
КонецФункции

#КонецОбласти

#Область Инициализация

#КонецОбласти

Функция СведенияОВнешнейОбработке() Экспорт

    ИмяОтчета = ЭтотОбъект.Метаданные().Имя;
    Синоним = ЭтотОбъект.Метаданные().Синоним;
    Синоним = ?(ЗначениеЗаполнено(Синоним), Синоним, ИмяОтчета);

    РегистрационныеДанные = Новый Структура;
    РегистрационныеДанные.Вставить("Вид", "ДополнительнаяОбработка");
    РегистрационныеДанные.Вставить("Наименование", Синоним);
    РегистрационныеДанные.Вставить("Версия", ВерсияОбработки());
    РегистрационныеДанные.Вставить("БезопасныйРежим", Ложь);
    РегистрационныеДанные.Вставить("Информация", "Анализ и контроль доступа");

    ТаблицаКоманд = ПолучитьТаблицуКоманд();

    РегистрационныеДанные.Вставить("Команды", ТаблицаКоманд);
	 
	// Ручной запуск
	ДобавитьКоманду(ТаблицаКоманд, "Открыть обработку", "ОткрытьОбработку", ДополнительныеОтчетыИОбработкиКлиентСервер.ТипКомандыОткрытиеФормы(), Истина);

    Возврат РегистрационныеДанные;
	
КонецФункции     

Функция ПолучитьТаблицуКоманд()

    Команды = Новый ТаблицаЗначений;
    Команды.Колонки.Добавить("Представление", Новый ОписаниеТипов("Строка"));
    Команды.Колонки.Добавить("Идентификатор", Новый ОписаниеТипов("Строка"));
    Команды.Колонки.Добавить("Использование", Новый ОписаниеТипов("Строка"));
    Команды.Колонки.Добавить("ПоказыватьОповещение", Новый ОписаниеТипов("Булево"));
    Команды.Колонки.Добавить("Модификатор", Новый ОписаниеТипов("Строка"));

    Возврат Команды;

КонецФункции

Процедура ДобавитьКоманду(ТаблицаКоманд, Представление, Идентификатор, Использование = "ОткрытиеФормы", ПоказыватьОповещение = Ложь, Модификатор = "ПечатьMXL")

    НоваяКоманда = ТаблицаКоманд.Добавить();
    НоваяКоманда.Представление = Представление;
    НоваяКоманда.Идентификатор = Идентификатор;
    НоваяКоманда.Использование = Использование;
    НоваяКоманда.ПоказыватьОповещение = ПоказыватьОповещение;
    НоваяКоманда.Модификатор = Модификатор;

КонецПроцедуры


// Telegram: @QIQTPP
// Email: denqiqtp@gmail.com
// Github: https://github.com/VorozhbitDM/Access-control-1C
